# Task Plan: 修复 Agent 大块输出与长任务滚动卡顿

## Goal
确定剩余大块输出发生在provider、SSE还是前端提交层，并让长对话的离屏消息、折叠工具结果不再持续参与布局与绘制。

## TODO—验收对应
- [x] T1：从最新真实消息诊断provider正文chunk大小、数量和终态补齐差值。
- [x] T2：建立能复现大块提交与滚动长任务的前端性能测试。
- [x] T3：修复确认的批量输出根因，不以逐字动画伪装流式。
- [x] T4：离屏完成消息和折叠大结果停止布局/绘制，保持展开与复制能力。
- [x] T5：滚动事件不触发不必要的响应式更新或同步布局。
- [x] T6：真实界面验证长消息连续输出、上下滚动和按钮响应。
- [x] T7：更新当日变更日志与验收报告，完成前重读TODO和开发规范。

## Phases
- [x] Phase 1：读取真实诊断并建立性能基线
- [x] Phase 2：建立失败测试并定位主线程热点
- [x] Phase 3：实施流提交与离屏渲染修复
- [x] Phase 4：串行回归、真实界面冒烟与规范复核

## Decisions Made
- 优先使用浏览器原生 `content-visibility` 和现有草稿缓冲，不引入虚拟列表依赖。
- 只有证据表明provider产生整块chunk时，才在后端增加安全拆分/节奏控制。

## Errors Encountered
- 首轮联合测试中子Agent自动唤醒用例偶发得到2次唤醒；同批修复后重跑全部31项通过，确认不是本次滚动/流提交路径的稳定回归。

## Status
**Complete** - 代码、串行回归、生产构建、真实界面验收、文档和最终规范复核均已完成。

---

# Task Plan: 子 Agent 终态提示去重与 DeepSeek 思考回传修复

## Goal
每个子 Agent Turn 的终态只产生一次用户可见提示，并确保带工具调用的 DeepSeek 思考消息在下一次模型请求中完整回传。

## TODO—验收对应
- [x] T1：创建 N 个子 Agent 时，每个 `run_id`/Turn 的终态提示恰好一次。
- [x] T2：DeepSeek thinking 模式的 assistant tool-call 消息在 tool 结果后的请求中保留 `reasoning_content`，不再返回 400。
- [x] T3：两条缺陷均先由失败测试复现，再修复并串行回归。
- [x] T4：完成实际 Agent 界面冒烟，报告任何无法验证项。

## Phases
- [x] Phase 1：读取规范、技能并梳理双链路
- [x] Phase 2：建立真实失败测试
- [x] Phase 3：实施最小根因修复
- [x] Phase 4：串行回归与实际界面冒烟

## Decisions Made
- 保留当前工作区全部既有未提交改动，不覆盖或回滚。
- 去重必须基于稳定事件身份，不依赖单个前端 store 的瞬时状态。
- reasoning 验证必须覆盖 assistant tool_calls + ToolMessage 的连续请求。

## Errors Encountered
- 初次 `rg` 在 `cmd.exe` 中使用含管道符的正则，被 shell 解释；已改为逐个固定模式查询。
- 前端定向用例最初把 Vitest `-t` 参数放在错误位置，导致全项目用例被收集但目标被跳过；已改为按测试文件串行执行。
- `test_agent_core_service.py` 有 10 个无关既有失败；本次相关定向套件均通过。
- 全量 `vue-tsc --build` 被仓库无关既有类型错误阻塞；本次文件定向 Vitest/E2E 通过。

## Status
**Complete** - 两条用户反馈均完成实现、失败基线、回归与实际界面验收。

---

# Task Plan: 修复 Agent 历史保存与加载重复

## Goal
历史恢复必须与一次真实对话的语义消息一一对应，不重复显示循环中的等待工具、思考或中间回答。

## TODO—验收对应
- [x] T1：确认截图会话的重复记录究竟存在于数据库、历史 API 还是仅由前端恢复放大。
- [x] T2：修复共享持久化/恢复根因，禁止用前端文案去重掩盖坏数据。
- [x] T3：覆盖 Agent 多循环、重复工具等待、历史重载和当前已有坏数据的兼容恢复。
- [x] T4：实际界面重新加载会话，重复等待块不再出现。

## Phases
- [x] Phase 1：读取规范与调试技能
- [x] Phase 2：核对真实数据库与保存/加载链路，建立失败测试
- [x] Phase 3：实施最小根因修复
- [x] Phase 4：串行回归与历史页面冒烟

## Decisions Made
- 数据库、API、前端三层逐层计数；不先假设前端有错。
- 保留现有未提交改动，不回滚其他任务。

## Errors Encountered
- 本地完整 FastAPI lifespan 启动被仓库现有迁移/启动状态阻塞，改用正式 MessageService 直读真实 SQLite，并用 Chromium 验证前端重载。
- 首次历史 E2E 重载时 session 行被活动栏遮挡，改为 DOM 语义点击后通过。

## Status
**Complete** - 真实坏数据、执行循环和历史语义投影均已修复并验收。

---

# Task Plan: Agent 对话来源、结果块布局与库菜单交互修复

## Goal
让来源引用始终在右侧边栏编辑区打开，让四类 Agent 内容块独立分行，并让左侧库二级菜单点击外部后自动收起。

## TODO—验收对应
- [x] T1：点击每次对话底部“来源”中的本地或四库来源，在右侧边栏模式打开内容，不跳转到主编辑区。
- [x] T2：挂载文件、图书馆、文献阅读、组件库四类块分别占据独立行，不混排。
- [x] T3：左侧边栏库按钮展开二级菜单后，点击菜单外任意区域自动收起。
- [x] T4：为三项缺陷分别建立回归测试并通过实际界面冒烟验证。
- [x] T5：重读 TODO 与开发规范，记录变更和未完成项。

## Phases
- [x] Phase 1：读取规范与技能，建立逐项验收映射
- [x] Phase 2：定位三个交互的真实组件和共享调用链
- [x] Phase 3：先建立失败测试，再实施最小根因修复
- [x] Phase 4：串行回归、构建与实际界面冒烟

## Decisions Made
- 复用搜索页面已有分裂模式和现有右侧编辑区入口，不另建平行组件或导航协议。
- 保留工作区现有文件与历史记录，只修改本次交互直接涉及的代码。
- 来源区块统一调用 `openEditorSidebar`，网页来源继续使用既有外部浏览行为。
- Agent挂载结果按 `SEARCH_SOURCES` 顺序分组，复用 `SearchNativeResultCard` 和搜索页同名布局语义。
- ActivityBar使用一个组件生命周期内的document `pointerdown`监听，仅在点击当前展开组之外时收起。

## Errors Encountered
- 首次追加规划文件时匹配了不存在的旧记录末行，补丁整体未应用；已改用精确锚点重试。
- 首次测试补丁对同一文件使用了两段更新，被补丁工具拒绝且未写入；已合并为单段更新。
- 首轮失败基线为3个新增用例失败、17个既有用例通过；最终同批21个用例全部通过。
- 全量 `vue-tsc --build` 被仓库已有类型错误阻塞；错误列表未包含本次3个组件或3个定向单元测试。
- 精确恢复Vitest缓存时前两次Node命令被Windows引号转义拒绝且未写入，第三次使用单引号脚本成功恢复，缓存已从diff移除。
- 端口归属检查时系统没有`wmic`；改用`tasklist`确认5173由已有Node进程占用且累计CPU时间明显早于本轮冒烟，Playwright复用了该服务，未关闭用户进程。

## Status
**Complete** - 三项交互已完成根因修复、失败基线、定向回归、生产构建和实际界面验收。

---

# Task Plan: 四库 K 引用侧栏导航

## Goal
让正文与来源区中的 `[K#]` 引用覆盖文件库、图书馆、组件库和文献库，并像对应挂载块一样打开各自类型的右侧侧栏。

## TODO—验收对应
- [x] T1：`K链接应该扩展到四个库` → 四类 `search_result` 均通过 `[K#]` 精确路由，历史消息继续使用持久化 citation metadata。
- [x] T2：`点击效果均应该像挂载的块那样` → 正文 `[K#]` 与底部来源统一复用 `openSearchResultSidebar`，不新建导航协议。
- [x] T3：`点击后展开各自库类型的侧边栏` → 文件库打开右侧编辑器；图书馆、组件库、文献库打开现有原生搜索结果侧栏。
- [x] T4：`README里面补充说明K链接可以挂载四库的源` → 更新引用编号与点击行为说明。
- [x] T5：为正文与来源区建立失败回归，完成实际界面四库点击冒烟并重读规范。

## Phases
- [x] Phase 1：读取规范、技能并确认现有 citation metadata 与共享侧栏路由
- [x] Phase 2：建立失败测试并实施最小共享路由修复
- [x] Phase 3：更新 README 与变更记录
- [x] Phase 4：串行回归、构建、实际界面冒烟和规范复核

## Decisions Made
- 四库 K 引用以已持久化的 `SourceItem.search_result` 为唯一精确路由依据。
- 复用挂载块已有的 `openSearchResultSidebar`，旧本地引用、网页与附件保留原兼容行为。
- `MarkdownContent.vue` 已超过项目 1000 行上限；修改时仅抽离现有高亮主题 CSS，避免顺带重构业务逻辑。

## Errors Encountered
- 首次追加规划与测试的原子补丁因既有任务同步完成、规划文件锚点变化而整体未写入；重新读取后按最新尾部追加。
- 首次用 `npm exec` 运行定向测试时参数被Windows npm转发吞掉，误收集全量测试；已中止并改用本地 `vitest.cmd` 严格单文件串行执行。

## Status
**Complete** - 四库K引用正文与来源区导航、README、定向回归、构建和实际界面验收均已完成。
# Task Plan: Agent 全工具可用性审计与修复

## Goal
枚举项目实际注册给 Agent 的全部工具，以低内存串行测试逐项验证；所有失败工具修复后复测通过，并留下工具级验收证据。

## TODO—验收对应
- [x] T1：从正式注册链路生成 Agent 全部工具的唯一清单，覆盖内置、扩展与 MCP 动态工具。
- [x] T2：每个工具至少有一项可执行的成功路径测试；有副作用或依赖外部环境的工具使用受控临时资源或协议级测试。
- [x] T3：所有不可用工具均先由失败测试复现，再做最小根因修复并逐项复测。
- [x] T4：测试严格单进程、单 worker、分模块串行执行，不进行高内存全量并发测试。
- [x] T5：必要的模型 API 调用最小化；临时 key 不写入仓库、日志或验收报告。
- [x] T6：完成实际 Agent 界面工具调用冒烟，重读 TODO 与开发规范，并报告逐工具结果和任何未完成项。

## Phases
- [x] Phase 1：梳理正式注册链路并建立全工具清单
- [x] Phase 2：盘点既有测试覆盖并补齐低成本失败基线
- [x] Phase 3：修复失败工具并逐模块串行复测
- [x] Phase 4：实际 Agent 冒烟、变更记录与最终逐项复核

## Decisions Made
- “所有工具”以运行时正式暴露给 Agent 的注册结果为准，不以文件名或文档列表代替。
- 外部服务、破坏性操作和长任务工具必须使用 mock、临时目录或只读资源验证协议与执行链路，不对用户真实数据执行副作用。
- 保留现有未提交的 `editor/e2e/wiki-links.spec.ts`，不修改、不回滚。

## Errors Encountered
- Git Bash 在当前 Windows 主机触发 WSL 网络错误，后续命令改用 `cmd.exe`；文件修改仍统一使用 UTF-8 补丁。
- MCP 基线测试断言已移除的 `echo_text`；修正为正式 `list_available_tools` 后通过。
- 新增逐工具测试首轮 5 项失败均为测试替身接口名或返回形状不匹配；按正式调用链校正后 13 项全部通过。
- Playwright 首次传完整路径未收集测试；改用 testDir 内文件名后正常收集。
- Agent 工具预览测试残留不存在的 stream DOM class 与旧 shimmer 本体 CSS 断言；按当前实现更新后 Tool/Chat 两项通过。

## Status
**Complete** - 107/107 原生工具、MCP 动态适配层、生产构建和实际 Agent 界面均完成串行验收，无未完成项。

---
# Task Plan: Agent 全工具真实 API 验收与修复

## Goal
通过真实 FastAPI lifespan 和 `/agent/stream` 模型循环逐项调用全部 Agent 工具；成功项永久跳过，失败项修复后只复测失败集合，直至全部成功。

## TODO—验收对应
- [x] A1：真实启动应用生命周期，不 mock Service、不拦截 HTTP、不直接调用工具函数。
- [x] A2：让模型经 `/agent/stream` 实际产生 tool call，并按 `tool_call_end` 记录每个工具的 API 成功或失败。
- [x] A3：首次成功的工具写入持久账本，后续批次不得再次调用，避免重复 token 消耗。
- [x] A4：失败工具先保留真实 API 失败证据，再修复共享根因并只复测失败项。
- [x] A5：107 个原生工具全部 API 成功；MCP 只验收项目可控的真实 API 注册/调用链，外部 server 业务不伪造。
- [x] A6：测试串行、小批次执行；模型 key 不写入仓库、命令、日志或报告。

## Phases
- [x] Phase 1：确认真实 API、模型配置、会话与工具选择协议
- [x] Phase 2：建立可恢复 API 账本与首批真实调用
- [x] Phase 3：循环修复失败根因并仅复测失败工具
- [x] Phase 4：107/107 终验、端口清理、报告纠正与规范复核

## Decisions Made
- 之前的 mock/直调测试只保留为单元回归，不再作为“工具可用”的验收证据。
- API 通过的唯一标准是 `/agent/stream` 返回该工具对应的成功 `tool_call_end`，且没有 error/503。

## Errors Encountered
- 旧验收错误地用 service stub 绕过请求绑定，无法发现真实 API 的 503；本轮从零建立 API 证据。

## Status
**Complete** - 真实 `/agent/stream` API 逐工具终验 107/107 通过，密钥、会话、开关和端口均已清理。

---

# Task Plan: 组件库“绘图脚本”类型

## Goal
在不改变现有 Vue/HTML 组件预览的前提下，新增可持久化的“绘图脚本”上传模式、脚本语言与可选图片封面，并在卡片中按图书馆封面逻辑展示。

## TODO—验收对应
- [x] T1：上传表单左下角新增“绘图脚本”按钮，点击后有选中态且标签锁定为“绘图脚本”。
- [x] T2：绘图脚本模式主区域为左代码、右图片上传；图片可选，不上传时卡片用文字封面，且不编译脚本。
- [x] T3：组件名右侧增加“脚本语言”输入，支持自定义文本和内置下拉选项，以圆角胶囊形态展示。
- [x] T4：绘图脚本卡片不渲染实时预览，上方显示图片或文字封面；下方标签胶囊右侧显示脚本语言胶囊。
- [x] T5：脚本源码、脚本语言和封面关联通过正式后端服务/API/数据库迁移持久化，前端无假数据或本地存储。
- [x] T6：补齐服务、REST、前端 API 与组件测试，再在桌面/平板/移动尺寸完成实上传、列表和卡片冒烟。

## Phases
- [x] Phase 1：读取规范与相关技能，追踪现有组件库与图书馆封面链路
- [x] Phase 2：建立失败测试与正式持久化契约
- [x] Phase 3：实施上传模式、卡片和响应式布局
- [x] Phase 4：串行回归、真实代理请求、界面冒烟、规范复核与变更记录

## Decisions Made
- 普通组件保留现有 Vue/HTML 离线编译器；绘图脚本使用独立 `script` 源格式，提交和卡片都不调用编译器。
- 复用图书馆已有的封面上传资产与 `LibraryCoverUploader`，不新建重复图片管理链路。
- 绘图脚本的扩展元数据使用正式 SQLite 模型和 Alembic 迁移关联到知识库内的脚本文件。
- 内部标签值使用 `drawing scripts`，所有面向用户的位置显示“绘图脚本”。

## Errors Encountered
- 调查时尝试读取不存在的 `editor/src/api/http.ts`；实际共享请求客户端位于 `editor/src/api/client.ts`。
- 首次追加规划与笔记的联合补丁因 `notes.md` 锚点不匹配而整体未写入；已按两个文件的真实末尾分别重试。
- 首次合并多个失败测试的补丁因旧测试名锚点不精确而整体未写入；已拆成文件级补丁。
- 实现后首次服务回归暴露 `LibraryAsset` 在 session commit 后过期；改为该元数据事务使用 `expire_on_commit=False`，避免返回已脱离资产。
- 首次真实冒烟因侧栏分类与表单模式按钮同名而定位不唯一；改为对话框内精确定位后继续。
- 第二次冒烟发现 datalist 输入的 ARIA role 为 `combobox` 而非 `textbox`；改用稳定 `name` 定位。
- 768/480 截图首轮发现对话框层级低于 ActivityBar 且移动端溢出被截；提高遮罩层级并允许表单纵向滚动，重拍后字段和操作完整可见。
- 全量 `vue-tsc --build` 被仓库既有的 ImagePreviewer、SmartForms 等类型错误阻塞；错误列表未包含本次组件库文件。
- 定向 Python 编译通过；主机未安装 `ruff` CLI，改用定向 pytest、ESLint 和构建完成语法/规范验证。
- 隔离验收目录的前两次 PowerShell `Remove-Item` 调用被工具安全策略拒绝；在已独立核对绝对路径后使用 .NET 精确删除两个临时目录。
- 首次合并 README 与变更日志的补丁遇到同时任务已创建 2026-09-05 索引，因旧锚点失效而整体未写入；已重读后追加到当日文件。

## Status
**Complete** - 六项验收映射均已实现，定向回归、真实代理上传与三档响应式界面验收通过，无未完成项。

---
