# Agent 对话与侧边栏交互验收

本文记录 Agent 对话来源打开方式、四类结果块布局和左侧库菜单收起行为的实现对应与实际验证结果。

## 验收项

- [x] 来源点击在右侧边栏编辑区打开。
- [x] 挂载文件、图书馆、文献阅读、组件库四类块独立分行。
- [x] 库二级菜单点击外部后自动收起。
- [x] 正文与来源区的 `[K#]` 可精确打开文件库、图书馆、组件库和文献库的右侧侧栏。

## 验证记录

- 失败基线：三个新增单元用例分别复现主区跳转、四库混排和菜单不收起。
- 单元回归：`FinalTurnSummary.spec.ts`、`AgentSearchResultBlocks.spec.ts`、`ActivityBar.spec.ts`共21项通过。
- 实际界面：Chromium单worker运行2个工作区场景，验证来源右侧打开、Agent主区保留、四库section顺序与纵向几何、库菜单外部点击收起，2项通过。
- 视觉检查：桌面截图中四类原生卡片位于独立section，没有横向混排或横向溢出。
- 生产构建：Vite构建成功。
- 全量类型检查：被仓库已有类型错误阻塞，错误列表不包含本次修改的组件和定向测试。
- 定向ESLint：0错误；同一既有E2E文件保留1条与本次无关的`force: true`警告。
- 四库K引用单元回归：正文13项、来源区4项、历史消息元数据恢复3项通过；后端四库引用注册3项通过。
- 四库K引用实际界面：Chromium依次点击 `[K1]` 至 `[K4]`，四类原生右侧侧栏均可见且Agent主区保留，1项通过。
- 边界验证：图书馆结果的 locator 为HTTP地址时仍优先打开图书馆侧栏，不误跳外部浏览器。
