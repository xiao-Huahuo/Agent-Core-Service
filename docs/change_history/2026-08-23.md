# 2026-08-23

- Markdown Edit/Preview/Split 工具栏新增“目录树”按钮，章节侧栏以内嵌抽拉层呈现在编辑大卡片内；完整移植 Origin UI Vue 的 `Tree/TreeItem/TreeItemLabel` 三层封装，并由 Reka UI 提供层级、键盘和展开语义。
- 章节树解析 H1–H6 ATX 标题并忽略 fenced code，空 children 显式映射为 `undefined`，确保只有真实分支渲染箭头、叶节点只保留对齐占位；缩进线限定在 Markdown 六级标题范围并由标签背景遮住行缝。
- 顶栏取消分隔边框；全部展开/全部折叠复用文件树动作色彩和 hover 规格，搜索框复用文件树 `.tree-search` 的尺寸、圆角、边框与聚焦状态。
- Edit 模式按 textarea 光标定位当前章节，Preview 模式按渲染标题位置定位；当前章节使用主题色，标题悬停使用柔和主题色，点击标题平滑滚动，跨 Edit/Preview/Split 切换保持章节位置。
- Save 与目录树按钮统一工具栏样式；Save 增强软盘路径 hover 形变和按压缩放反馈，侧栏使用 transform/opacity 抽拉，分支使用 200ms 箭头旋转，并覆盖精细指针和 reduced-motion 条件。移除与 Reka 扁平树冲突、会把折叠标题压成竖排的 TransitionGroup 离场动画。
- 新增标题解析、树交互、叶节点无箭头、光标跟随、Preview 标题定位和 Chromium 浅色界面冒烟回归；32 项相关单测、Vite 构建及 Chromium 冒烟通过。
- 普通表格复用智能表格外骨架、列拖拽行和边缘增行/增列交互；阅读态与编辑态统一使用随全局字号缩放的 13px 表格字体，空行、一行文本、标签、星级与日期控件采用约 31px 的适中密度，多行文本按实际行数增长。
- 已有普通表格加载时会根据内容重新计算行高，清除旧编辑器曾持久化的 56px 空行/单行高度；单行 textarea 与 4px 边界拖拽热区避免编辑态再次异常撑高或遮挡双击。
- 文献内容折叠时先触发行高过渡再切换截断内容，折叠预览不再参与表格固有高度计算，展开和闭合均有连续动效。
- 文献内容阅读层增加独立的裁切、位移和透明度展开/闭合动画，不再把不可靠的 table-row 高度 transition 当作唯一动效。
- 智能表格左上操作改为“上传文献”，点击后新增一行并立即打开系统文件选择器；普通表格继续显示“新建行”。
- 所有非序号列支持从字段图标展开/收起辅助描述并双击编辑；新增列菜单提供辅助描述输入，描述通过正式数据库字段持久化并传入结构化 AI 生成请求。
- 辅助描述图标的单击操作延迟到双击判定结束，避免第一次点击改变表头布局后丢失双击；编辑框使用列级元素引用精确聚焦，并阻止表头拖拽截获输入操作。
- 辅助描述编辑框恢复失焦提交，确保内容未变化时也能退出编辑态；Enter 提交与 Esc 取消行为保持一致。
- 新增旧库字段迁移、列模型、生成请求、交互单测与 Chromium 界面回归，覆盖辅助描述、上传、折叠中间帧和各类普通单元格实际高度。
- [x] 为Markdown模式制作章节侧边栏:
  - 给顶部的save按钮加上悬停动效和点击动效.
  - 侧边栏在工作区大卡片的内部,在Markdown格式的Edit/Preview/Split toggle右边加一个按钮"目录树",样式和右边的save按钮一致.
  - 侧边栏样式和布局: 侧边栏背景颜色和编辑区大卡片完全一致,左侧有一个竖线用于隔离.采用以下的树组件样式和动效作为大小标题的结构.当前光标所处的位置所属的标题显示为主题色,文本悬停时显示为柔和主题色,点击一个标题则平滑滚动到此标题处.
  - 侧边栏的抽拉也要有平移动效.
  - 有一个顶栏,顶栏有两个按钮:全部展开,全部折叠.还有一个搜索框,用于全文搜索标题内容. 匹配的显示为加粗.
- [x] 继续优化智能表格:
  - 普通表格中,把表项的高度压缩到N行文字以及上下空出的少量间隙.尤其是无文字状态和一行文字的状态,尽量压缩宽度到Excel应用的效果,免去一切不必要的冗余高度. 标签列和星级列和日期列的高度也应和文本表项高度逻辑类似. 
  - 文献内容列表项不仅需要展开动效,也需要闭合动效.
  - 将普通表格的外骨架样式和交互对齐到智能表格.
  - 智能表格状态下,左上角的"新建行"应该调整为"上传文献",点击后新建一行并打开资源管理器选择文件进行上传.
  - 所有表格自定义列新增"辅助描述":
    - 右键菜单的 添加列-左/右侧添加 的三级菜单,其底部输入框下面应该再加一个输入框,名为"辅助描述". 辅助描述会在此列智能生成的时候被追加喂给AI.
    - 单击列名左边的图标可以在列名下平滑的展开和关闭辅助描述行.双击可以直接编辑.
    - 所有的列(除了行号列)都可以加辅助描述.
  - 列名行有点靠左上,应该靠左但是竖直应该居中,其图标和标签也是.
