# 2026-08-30 搜索来源视觉与锚定修正

## Agent 思考条(Think)展示

- 后端流式链路新增 `reasoning_delta`: `LLMTaskScheduler._stream_chat_request` 把 DeepSeek 返回的 `reasoning_content` 与正文分开 yield,不再被丢弃;`model_decision` 主 Agent 节点与 `model_runtime` 简单模式均累积思考文本并通过新增的 thinking 回调(thread-local,见 `tools/runtime_context.py`)实时推送。
- `graph_runner` 增加 `thinking` SSE 事件类型(与 `delta` 并列),前端据此流式渲染;简单模式落库 `metadata_json.reasoning_content`,主 Agent 路径落库时把片段列表统一拼接为字符串。
- 前端新增 `ThinkRow.vue`(结构/交互借鉴 DSH 的 `ReasoningRow`): 大脑图标 + "思考" 标题 + 折叠态摘要行(运行中跟随最新一行、结束后固定首行)+ 展开态全文,运行中带扫光动画并尊重 `prefers-reduced-motion`;对话模式(`ChatBubble`)与工具模式(`ToolBubble`)均渲染。
- chat store 新增消息 `thinking` 字段与独立 rAF 缓冲冲刷(与正文缓冲互不干扰),SSE `thinking` 事件累积到当前助手消息,历史加载从 `metadata.reasoning_content` 水合。
- 样式对齐工具条: 图标 15px、文字 `calc(12px * var(--font-scale))`、行间距 `--space-8`、行高 `--line-height-normal`,与 `ToolCallInline` 工具行一致。
- 持久化闭环: 思考文本随 `metadata_json.reasoning_content` 落库(主 Agent、简单模式、子 Agent 同一路径);历史加载水合;导出 `formatMessages` 原样携带 metadata,导入 `_import_messages` 原样保留,JSON 与 YAML 两种导入均不二次丢弃。
- 定向验证: 后端 `test_task_scheduler.py` reasoning_delta 单测 + 新增 `test_agent_thinking_persistence.py`(落库字符串/片段列表拼接/导入保留,共 15 passed);前端 chat store、ChatBubble 与 sessionExport 新增思考条/水合/导出测试(49 passed);vite 构建通过;真实界面冒烟依赖运行中的应用,见会话报告。

## 子 Agent 完整对话观测

- 修复后台子 Agent 完成后自动唤醒主 Agent 的消息元数据未落库问题；`wakeup` 与 `child_agent_event` 现在随 SSE 请求持久化，历史恢复继续显示为子 Agent 完成事件，不再退化成用户气泡。
- 子 Agent 运行改用归属于主会话的正式子 Session 持久化完整消息、工具调用和节点轨迹；左侧历史仅查询根 Session，删除主会话会同步清理子对话。
- 子 Agent 名称改为详情链接，点击后在右侧打开复用 Agent `MessageList` 的只读完整对话栏；桌面并排、平板覆盖、移动全宽响应式呈现。
- 子对话按 Session ID 隔离状态并发预载，单个请求失败不影响其他并行子 Agent；运行中对话独立轮询更新。
- 主会话 YAML 导出嵌套携带每个子 Agent 的完整消息，导入时重建父子 Session 关系与消息顺序；新增数据库迁移及前后端定向回归。

## Windows 发布打包完整性修复

- PyInstaller 明确收集 Alembic 环境、迁移版本与根配置文件，并停止全量收集 `agent_service` 子模块，避免发布 exe 因迁移目录缺失而启动失败；前端静态资源使用构建级临时快照，避免并发 Vite 构建替换哈希文件后生成残缺 exe。
- 敏感词库统一通过 `AgentConfig.storage.project_root` 下的外置资源路径读取、保存与热重载，确保 Electron 安装版中的用户修改可持久化。
- 显式声明代码直接使用的 NumPy、PyYAML、xlrd 与 fpdf2 运行依赖，避免依赖构建机上的偶然传递安装结果。

- 四库筛选统一使用文件、图书、组件、文献图标与蓝、黄、红、绿四种主题颜色；大搜索框显示图标和库名，顶栏小搜索框只显示图标，菜单内点击不再被误判为外部点击。
- 左侧“库”菜单六个入口分别使用独立颜色，文件、图书、组件、文献四项与搜索来源保持完全一致；密码库使用青色，智能表格使用全局蓝红混色。
- 搜索下拉菜单在打开期间实时跟踪各自输入框位置，覆盖顶栏展开动画、只读编辑侧栏关闭及工作区列宽变化，清空查询后仍锚定在大搜索框下方。
- 统一结果右上角改为彩色来源图标和库名并移除“来自”；统一/分裂切换增加滑动指示条，并固定滚动条占位，切换前后大搜索框几何尺寸与坐标保持一致。
- 定向 Vitest、TypeScript 检查和 Chromium 冒烟通过；浏览器实际量测两个菜单锚点、来源按钮常驻、滑块位移、搜索框零像素变化，并复查桌面、平板、移动、超窄截图。

## 四库可编辑搜索侧栏

- 搜索框和搜索页加载状态统一改用智能表格表项的公共 `PixelLoader`。
- 搜索结果统一复用 EditorWorkspace 既有编辑侧栏列与拖宽 resizer：文件使用 `EditorPane`，图书使用嵌入式 `LibraryItemDialog`，组件使用可编辑 `ComponentLibraryDetail`，文献使用默认展开的 `LiteratureEntryCard` 字段编辑器。
- 提取公共 `EditorSidebarCloseButton`，文件及三种业务侧栏右上角使用同一关闭控件；移动端编辑侧栏采用固定悬浮卡片并保持单侧栏互斥。
- 补齐组件源码增量更新的 REST/gRPC/前端 API 契约；图书、组件、文献保存后只原位更新当前搜索结果，不调用搜索接口。
- 完成四类桌面侧栏、拖动宽度、组件源码编辑、文献字段编辑和移动悬浮截图验收。

## 文献搜索视图与文件中图标修正

- 搜索页文献结果块关闭字段展开能力，点击只负责打开文献编辑侧栏。
- 文献侧栏右上角新增“字段明细 / 内容”滑动切换；字段明细保留文献字段编辑器，内容复用文献阅读页同款 `EditorPane`。
- 打开文献搜索结果时同步选择真实附件，内容模式可以直接使用对应文件的 Preview/Markdown/Edit 管线。
- 文件库中图标放大到 52px，文件名保留两行，容量文字降为 10px 基准，并限制卡片内部溢出，修复窄分屏名称遮挡。
- 新增文献不可展开、侧栏双模式和中图标窄布局回归测试，并通过桌面、分屏、移动端 Chromium 截图验收。

## Agent 流调度竞态修复

- SSE 客户端在积压事件批次中定期让出浏览器任务队列，避免工具调用后的连续微任务阻塞计时、按钮交互与 Vue 文本刷新。
- 让步后立即检查取消信号，防止用户中断后继续消费同一网络块中的陈旧事件；事件顺序保持不变。
- 新增共享流客户端定向回归和 Agent 页面 Chromium 冒烟，覆盖计时推进、流中侧栏交互、工具结果更新与最终回答多阶段呈现。

## Agent 用户消息时间与附件历史

- 移除 `get_current_time` 内置工具；每轮用户提问使用同一个 UTC 时间同时构建模型 `HumanMessage` 与持久化消息，历史提问也从 `created_at` 恢复时间标记。
- Agent 前端在聊天/工具两种模式下于用户气泡左侧显示灰色时间；移动与超窄宽度显示 `HH:mm`，完整时间保留在语义化 `datetime` 属性中。
- 本轮附件随流式请求提交，后端按用户与会话校验后写入用户消息元数据；历史加载、YAML 导出及 JSON/YAML 导入均保留附件关联和原始消息时间。
- 后端定向回归、前端 API/Store/导出组件测试及 Chromium 桌面、平板、移动、超窄真实界面冒烟通过；全量 TypeScript 检查仍被工作区既有类型错误阻塞，本次涉及文件未新增类型错误。

## Markdown Split 滚动同步修复

- 修复 Split 模式左侧滚动错误地按静止光标锚定右侧预览、导致预览只移动一小段后停止的问题。
- 编辑器滚动改为按两侧各自完整可滚动范围的归一化比例同步；光标移动、目录跳转仍保留语义块锚定。
- 新增编排层回归测试和真实长 Markdown 双向滚动测试，覆盖 25%、75%、底部及右侧反向 40% 同步。

## Agent 四库联合搜索工具修复

- `search_knowledge` 不再维护旧的单文件库搜索实现，改为复用前端 `/search` 同源的 `UnifiedSearchService`，默认覆盖文件库、图书馆、组件库和文献库，并支持按 `sources` 限定范围。
- 搜索框“AI 帮你搜索”会把当前库选择、全文开关和语义开关写入 Agent 请求，工具结果继续使用前端现有可解析的编号与引用格式。
- 后端工具/四库服务回归、前端搜索框/工具展示回归及真实 Chromium 搜索框交互冒烟通过；全量 TypeScript 检查仍被工作区既有无关类型错误阻断。

## Agent 四库搜索结果块挂载

- 四库搜索引用新增完整 `search_result` 元数据；Agent 最终回答引用 `[K#]` 时，消息历史会持久化并恢复对应的可点击结果块，未被引用的命中不会强制挂载。
- 提取 `SearchNativeResultCard` 作为搜索页分裂模式与 Agent 消息的公共渲染入口，文件、图书、组件和文献分别复用原有四类组件。
- Agent 主页面点击块继续打开搜索结果右侧编辑栏；工作区侧栏 Agent 点击后切换文件、图书、组件或文献主页面，并打开准确资源。
- 后端 9 项、前端 46 项定向回归和 ESLint 通过；完成桌面、平板、移动、超窄 Chromium 冒烟及主页面/侧栏两种点击验证。全量 TypeScript 仍仅报告工作区已有无关错误。

- [x] 去除掉"获取当前时间"的工具,改造提示词,将每次的用户提问标记好一个时间放进提示词(prompt里面,或者json).
  - 在前端的agent对话区,每次推出一个用户气泡,都应该在气泡左边用灰色字标注好时间.
  - 时间要记录进历史,加载历史的时候也要能够加载灰色时间字.导入导出也一样.
- [x] 修复agent历史加载没有加载用户气泡上挂着的附件的问题. 导入导出也一样.
- [x] 全库联合搜索的实现,文件库+图书馆+组件库+文献库 联合搜索,将结果放在同一个页面,可筛选某一种.
  - 任何搜索结果都应该有两种形式,一种是统一样式,也就是现在的搜索结果样式,但是需要针对三库进行扩展适配和弥补,右上角还要写明来自什么库;另一种则是分裂样式,将文件资源管理器的中图标+图书馆的卡片+组件库的组件卡片+文献库的左侧边栏文件块 一起堆在一起. 不好堆叠,建议竖着分成四块区域.  然后搜索框下拉菜单就可以除了原本的两个标签之外再新增四个标签,分别选中四个库.
  - 标题搜索+全内容部分匹配搜索+全内容语义搜索,三个搜索在四个库里面联合实现


- [x] 想个办法让Agent可以像挂载图片和文件一样挂载搜索页面的四库各自的"块".(可以改提示词,怎么让Agent会挂载图片的就怎么类似的让它学会挂载块.)
  - 搜索页面里面已经包含的四个库,除了文件库之外,剩余的三个库的内容就直接用搜索页面分裂模式的那些组件呈现.
  - 点击挂载的块则和搜索页面里点击四个库的块一样的效果,展开右侧边栏.
  - 如果是侧边栏模式的Agent,那么点击块就切换主页面到对应的库并打开即可.
- [x] 子agent任务完成落库修复: 意外的发现加载对话的时候会把子Agent的任务完成气泡也加载出来弄成用户气泡,修复这个逻辑.
- [x] 子Agent观测加强:
  - 问题: 在Agent页面点击环境变更会展开包含子Agent区块的侧边栏.但是这些子Agent只能展开,不能点击查看详情.
  - 需求: 子Agent的名字应该显示为一个链接,点击链接就展开右右边栏,里面再放一个复用的Agent页面组件(要求继承响应式),显示子Agent的完整对话.
    - 不同子Agent是并行的,则这些对话的加载也应该是多线程互不干扰的. 而且子Agent的对话不进入左侧的历史,但是要进入主对话的历史,能够在加载历史的时候也加载出来,同理可以随着主对话导入和导出.
