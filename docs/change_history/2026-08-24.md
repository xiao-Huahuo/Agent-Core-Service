# 2026-08-24

## 智能表格填充提速与移动菜单

- 将用户点击触发的结构化生成从后台事实队列改为前台交互队列，继续使用小模型层，避免智能填充等待低优先级后台任务；结构化生成服务 6 项测试通过。
- 智能表格右键菜单新增“行移动”和“列移动”二级菜单，分别复用既有行上移/下移、列左移/右移逻辑；新增组件回归并通过 Chromium 实际菜单冒烟。

## 变更历史文档拆分

- 将原 `docs/CHANGE_HISTORY.md` 的 49 个唯一日期段拆分为 `docs/change_history/YYYY-MM-DD.md`，正文逐段校验后删除旧聚合文件。
- 新增 `docs/change_history/README.md` 日期索引，并同步更新根 README 项目结构与开发规范中的日志写入路径。
