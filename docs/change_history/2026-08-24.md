# 2026-08-24

## 智能表格填充提速与移动菜单

- 将用户点击触发的结构化生成从后台事实队列改为前台交互队列，继续使用小模型层，避免智能填充等待低优先级后台任务；结构化生成服务 6 项测试通过。
- 智能表格右键菜单新增“行移动”和“列移动”二级菜单，分别复用既有行上移/下移、列左移/右移逻辑；新增组件回归并通过 Chromium 实际菜单冒烟。

## 变更历史文档拆分

- 将原 `docs/CHANGE_HISTORY.md` 的 49 个唯一日期段拆分为 `docs/change_history/YYYY-MM-DD.md`，正文逐段校验后删除旧聚合文件。
- 新增 `docs/change_history/README.md` 日期索引，并同步更新根 README 项目结构与开发规范中的日志写入路径。

## 知识图谱力导交互修复

- 语义图、文件树、图书馆和双向链接统一使用同一个语义式 simulation：相同链接距离与强度、节点斥力、碰撞、文档互斥、持续 alpha，并将共享速度衰减提高到 `0.62` 以增加地板摩擦；删除模式判定、层级环锚点及 x/y/center 力分流。
- 语义实体取消 8 倍异常斥力，文档斥力改为随 alpha 缩放；四种图均仅通过真实图关系形成自由力学分布，不再受画布中心方向影响。
- 节点拖拽不再使用持续升高全图温度的 `alphaTarget(0.22)`，只在按下时确保模拟运行，避免拖得越久其余节点越向外扩散；四拓扑力学契约及孤立文档平衡共 10 项测试、四模式 Chromium Canvas 冒烟通过。
- 四图共享力学新增相对自由节点质心的弱内聚力，与斥力形成有限平衡并压缩双向链接孤立文档间距；拖拽固定节点不参与质心计算，避免绝对中心锚定和拖一节点带动全图。

## 图谱人工中心清理

- 语义图谱不再注入知识库中心节点及其到文档的人工连线，直接呈现后端真实语义节点与关系。
- 图书馆图谱移除虚拟根节点，顶层书籍与集合保持无父节点，仅集合内部真实嵌套关系生成连线。
- 文件树保留层级所需根节点，但根节点改用原语义中心节点的 MetaWeave 图标；适配器回归 12 项及 Chromium 三模式视觉冒烟通过。

## 语义图谱初始圆盘种子

- 将语义节点的同半径圆周初始化改为确定性黄金角圆盘：角度按黄金角递增，半径按节点序号平方根铺开，避免后端连续节点在关系力收缩时残留为边缘月牙。
- 不修改链接、斥力、内聚力或摩擦参数；初始半径分布、相关力模型共 11 项测试及 Chromium 首次可见状态冒烟通过。

## 应用背景封面

- 外观设置新增“背景封面图片”上传与重置，上传成功立即应用 cover 背景；背景 URL 通过 `user_settings.background_cover_url`、运行时迁移、profile、REST 与 gRPC 正式持久化，浏览器缓存不保存该字段。
- 从图书馆创建/编辑表单提取 `LibraryCoverUploader`，三个入口统一复用原 `/library/assets/cover` 上传链路和持久化资产；重置保存空字符串并恢复默认应用壳。
- 后端 5 项、前端 15 项、Chromium 上传/应用/重置及 5173 真实代理请求验证通过。

## 侧边栏宽度拖拽

- 侧边栏编辑区与侧边栏浏览器新增与 Agent 一致的 4px 左边缘拖拽命中区，复用工作区现有 pointer capture、rAF 节流、全局光标和释放清理逻辑。
- 编辑侧栏使用 360–620px 范围，浏览器侧栏最小 320px；最大宽度动态保留至少 180px 主编辑区，向右拖过阈值调用现有关闭动作。
- 结构与状态单元测试 4 项、Chromium 两类侧栏实际拖宽及视觉冒烟通过。

- [x] 优化图谱显示逻辑:
  - 当观察双向链接图的时候发现,那些文档节点似乎会向四周围无限运动然后最终停止,而中间的链接体就像恒星一样几乎无限自转. 这个不是不好,只是说这个过程太过漫长,而且停止的地方(分散的小球的平衡点)太远了.
  - 当观察语义图谱的时候发现,一个文档节点的所有子节点会被顶到中心的另一边,但是我所希望的是这些子节点环绕在文档节点的周围,跟中心的所处位置无关.
  - 意外的发现是,当手动拽住文档节点的时候由近及远,发现子节点经过了全部远离-两侧平衡(四周分布)-全部逼近的一个过程.
  - 拽住一个节点向外拉,其他节点莫名其妙的也向外推,仿佛越远力越大,真是诡异的力导.
