# 2026-08-10
- [x] 将智能表格主数据从知识库 `form.json/data.csv` 文件迁移为 SQLite 关系表存储:新增 smart_forms/smart_form_columns/smart_form_rows/smart_form_cells 模型、服务和 REST API,前端改走数据库读写,附件仍保存在知识库 assets 并可灌库抽取。

- 修复管理栏模式知识库子菜单与相邻入口重叠；分组改为明确的纵向流布局并固定子菜单占位高度。
- 调整左侧活动栏顺序,将入库、MD-HTML 和 Skills 入口连续放到上方主标签区最下方。
- 调整知识库胶囊交互：图标模式悬停知识库按钮或胶囊区域即可展开,点击任一子项后自动收起。
- 修正图标模式知识库竖向胶囊内按钮的水平对齐,三个图标现在均位于胶囊正中。
- 为图标模式知识库竖向胶囊补充主题适配的窗口阴影,增强其与主内容层的分离感。
- 修复图标栏模式知识库竖向胶囊被主内容层遮挡的问题；提升活动栏与知识库子菜单层级,确保点击后可见且可交互。
- 修复图标栏模式知识库按钮点击无响应；知识库改用原语义图谱图标,图谱入口与图谱抽取按钮改用原知识库图标。
- 优化知识库导航图标与二级菜单：使用知识网络图标,图标模式改为独立纵向胶囊,管理模式固定展开尖角位置避免抖动。
- 将文件资源管理器、图书馆和智能表格归入左侧“知识库”二级菜单；保留原入口激活态并增加与侧栏模式一致的展开动效。
- 修复右键菜单在窄视口仍被右侧浮层遮挡的问题；菜单层级提升到应用浮层之上,并根据视口空间让二级菜单向左或向右展开,同时限制主菜单坐标不出屏。
- 修复主菜单向左展开时三级菜单仍向右展开的问题；所有右键子菜单层级统一继承主菜单展开方向,避免三级菜单与主菜单重叠。
- 修复连续点击不同表项智能提取时后续请求并发争抢共享 LLM 调度资源导致失败的问题；保留异步任务和单元格状态,同一表格的结构化生成请求限制为最多 2 个并发,其余受控排队执行,并补充连续多行提取回归测试。
- 在智能表格右上工具栏新增“清空无效字段”,统一清除失败或空单元格并保留正在生成的单元格及上传文件元数据。
- 修正“清空无效字段”按钮使用未注册的 `delete` 图标导致图标不显示的问题,改用已注册的 `trash` 图标。
- 支持用户自定义列名点击后原地编辑并自动保存；取消表格列的左侧冻结,横向滚动时所有列随表格移动。
- 修复标签编辑器点击输入框会被单元格拖选事件关闭的问题；增加编辑器关闭按钮,并禁止拖选单元格时浏览器选中文本,同时保留输入框文本选择能力。
- 允许删除文献上传/文献内容列；删除文献源后表格降级为普通表格,不再补回文献列,已有智能文本/智能标签列转换为普通文本/标签并隐藏智能填充入口。
- 将智能表格右键菜单及全部子菜单 Teleport 到 `body`,脱离工作区的 `overflow` 和绘制裁剪上下文,避免菜单在表格边缘被遮挡。
- 修复智能表格上传 DOCX、CSV、XLSX 等结构化文档时误报“暂未取得可显示文本”的问题；上传提取统一读取预览中的表格 sheets 和文档 HTML 文本。
- 修复智能表格多选块智能填充因关闭右键菜单清空选区而误报“没有可生成的智能列”的问题；多选和全表填充改为按行并发请求，并在每个请求中携带该行全部目标智能列，确保所有行列都能回填。
- [x] 新增通用结构化字段生成服务:增加 `/structured-generation/fields` REST 接口和 `StructuredGenerationService`,复用底层 LLM 调度与用户模型配置但不进入 Agent 对话流,后端负责 fenced JSON/嵌套 values/fields/results 多形态解析、字段级空值和标签选项校验;智能表格改为调用该通用接口并按单元格 generation token 合并结果,旧请求晚返回不会覆盖新请求。
- [x] 修复智能表格智能填充状态误报:模型无效 JSON、空结果和缺少文献内容不再被标记为已生成,单元格会写入 failed 状态并显示「失败/空」角标;成功/失败 toast 按实际数量反馈;智能填充点击后改为后台异步执行,允许用户继续选择或多次触发不同表项。
- [x] 智能表格补齐菜单与交换动效:表格切换、导出、标签筛选、星级筛选和布尔单元格下拉统一改为资源管理器排序菜单风格,菜单弹出和选项逐项出现使用同一组动画;右键菜单、子菜单和标签选择器同步加入弹出/逐项动效;列/行拖拽放下后增加短暂交换落位动画。
- [x] 修复智能表格标题列拖拽后自动弹回:前端 `normalizeForm` 不再强制把必需列恢复为默认顺序,只保留属性修正和缺失必需列补齐;拖动标题列后自动保存回写仍保持用户列顺序;补充归一化顺序回归测试。
- [x] 智能表格支持直接拖拽重排:表头列名可横向拖放重排列,序号列单元格可纵向拖放移动整行,放下后行数组重排且序号按当前顺序自动重新显示;补充列拖拽和行拖拽回归测试。
- [x] 智能表格补充单元格多选:支持按住鼠标拖选多个表项,右键落在已选表项上会保留选区并对选区批量执行清空、复制/粘贴、智能填充、删除整行和删除整列;智能填充仅处理被选中的智能列单元格,避免误填矩形范围内未选目标;补充拖选批量右键回归测试。
- [x] 智能表格补齐 ZIP 导出与入口命名:左侧 ActivityBar 表格入口显示改为「智能表格」;导出菜单新增 ZIP,无需新增依赖即可打包当前表格的 Markdown、CSV 与 `form.json` 快照;补充 ZIP 导出回归测试。
- [x] 智能表格改为编辑后自动保存:移除右上角手动保存按钮,表格内容、行列、标签和右键菜单修改会通过防抖自动写入数据库;保存响应按本地修订号回写,避免旧保存响应覆盖用户继续编辑的新内容;补充自动保存回归测试。
  - [x] 智能表格:面向**科研文献**的智能化字段管理.
    - 存储在知识库内的forms/之内,可以有多张表格,每个表格在forms/下有一个文件夹,文件夹内存储一个csv和一个assets/文件夹,assets/里面存放表格上传的实体文件.forms/像library/一样默认被系统忽略,但`forms/**/assets/**`不忽略..
    - [x] 表格可编辑,可以重排列和行,具有默认列,默认列可删除,也可以插入内置的选择性列,还可以插入自定义的列.
      - 所有列的内置类型:
        - 文本: 普通文本,可编辑.
        - 智能填充文本: 拥有文本的功能,并且自动将上传的文件内容和用户自定义的这个列标题通过LLM自动的异步生成文本,用户可编辑,编辑后可以点击格子右上角的图标按钮进行重新LLM生成,也可以直接保存.
        - 标签: 用户可编辑和增加和删除标签.
        - 智能标签: 拥有标签的功能,并且智能填充,具体生成过程是将现有所有此列的标签和提取的文字一起喂给LLM,LLM选择已有的(多个)标签或者自己生成新的标签.用户可编辑和增加和删除标签.
        - 选择框: 只能选择"是"或者"否",可编辑.
        - 星级: 只能选择1-5颗星,可编辑.
        - 日期: 可以调用待办侧边栏中的日历选择器输入日期和精确时间.
      - 默认列:
        - 序号(文本): 从1开始自动生成,不可编辑.
        - 文献上传(特殊列): 拖拽上传任何知识库支持的格式的文件,自动灌库并提取文字备用.一列只能上传一个文件.
        - 文献内容(文本,其他所有智能列的内容来源): 用户上传文件之后显示抽取的文字内容,不可编辑.
        - 标题(智能填充文本): 提取文字后,自动喂给LLM生成,可编辑.
      - 选择性列:用户可选择性的插入这些列.
        - 文献类型(智能标签): 智能自动填充的标签列,用户可编辑和增加和删除标签.
        - 重要性(星级): 以星级形式显示,默认是空的5颗星,用户可以随时打星.
        - 阅读进度(标签): 只能选择"未读","已读","阅读中"三种标签,默认是"未读",用户可修改.
        - 关键词|摘要|期刊|作者|年份|研究背景(why)|研究内容(what)|研究方法(how)|研究结果|创新点|局限性|未来展望|DOI|URL (智能填充文本): LLM会异步的自动从上传的文献中提取这些信息并填充到表格中,不同的列用不同的彩色浅背景区分.
      - 自定义列: 用户可以自定义列名和列的类型(仅提供内置类型),并插入到任何地方.
    - [x] 表格可以(针对某一列或者全表格内)进行匹配搜索(和可选的语义搜索),高亮符合的行.
    - [x] 表格可以筛选,筛选条例如下:
      - 标签筛选: 全表范围内筛选所有标签和智能标签类型的列,只要有一个标签符合就显示.
      - 星级筛选: 筛选1~5星的列.
    - [x] 可导出为csv文件(不导出文献列),也可以导出为xlsx,md表格(不导出文献列);还可以导出为zip,zip内包含导出的csv文件和文献列的所有文件.也可以从这几个格式中导入.
    - [x] 表格不一定要有文献列.如果用户选择删除文献列,那么就退化为普通表格,普通表格只能导入或者手动改列,不再享有智能列功能.
