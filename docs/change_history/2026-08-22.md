# 2026-08-22
- [x] 找出整个项目后端业务所有的硬数值限制:
  - 硬编码数值应该全部统一化放在AgentConfig里面统一配置.
  - AgentConfig里面每一条常量配置全都要备注其用途.
  - 系统提示词也归一化到`PromptConfig`中,不再散落在各处.
  - 修复 Agent 工具原始结果泄漏到中间输出及孤立工具消息导致的 400 错误；恢复 Agent 工作区 4px 半透明外框，并提供可拖动的消息滚动条。
  - 修复 Agent 侧边栏模式的窄宽度显示：经真实 Chromium 截图复核，精简挂载文件与修改摘要信息、消除欢迎品牌图对消息区的短暂覆盖、挂载文件改在主编辑区打开，并将输入工具条整理为无重叠的左右动作组；三条事后推荐从输入框上方迁到本轮 Agent 输出下方，由共享 chat store 在每轮流式输出结束后统一刷新，并在异步到达时保持底部跟随、禁止 flex 压缩，避免新会话中推荐消失或只剩一条边框线。
  - 如此,在后续可以将一些系统配置升级为用户配置.(现在不做)
- [x] 修理agent侧边栏模式的显示问题.响应式布局,应减少一些信息的显示,做到简洁扼要.
  - 文件挂载框显示问题; 侧边栏模式下文件挂载框点击之后应该跳转编辑区打开(而不是编辑区侧边栏).
  - 底部修改块显示问题.
  - agent输入框下面的按钮重叠的问题.
- [x] 更新debug页面内工具注册表的工具
- [x] debug页面增加一个子页面"全局常量",显示AgentConfig里面所有配置类的所有常量的介绍和数值(或者字符串).
  - 不硬编码,要做到如果后续更新AgentConfig常量,原来的代码仍然可以显示AgentConfig的全部配置.
  - 不可修改,仅展示,布局和样式应模仿现在的工具注册表和API页面.
- [x] 修复 Agent 长工具调用后的流式停滞与末尾大块输出：每次模型调用显式重置累计 token 基线；主窗口与悬浮窗改为双向 IPC，并按会话增量同步消息、流状态、取消信号和顶栏设置。悬浮窗展开态直接复用完整的侧边栏模式 `AgentPanel`，不再重复实现顶栏、消息区、输入区和抽屉；远端会话切换不再用空历史覆盖实时流，同时保护新建会话免受并发会话列表/历史请求回写。验证覆盖后端模型边界、前端状态同步、共享侧边栏 UI，以及 Chromium 中侧边栏流式、460px 实际小窗逐段输出、双窗口新问题/流状态同步与跨窗口中断。
- [x] 重建 Agent 上下文压缩：完整会话历史按稳定时序持久化，工作上下文使用模型 tokenizer 与 token 预算选择大窗口；Plan/ReAct 所有进入主模型的路径先走条件预算路由，仅超限时同步调用小模型合并重要事实、历史动作和未完成动作。压缩状态按版本写入 Session JSON，并在启用记忆时异步幂等写入长期记忆；失败时原子回退到安全滑动窗口。SSE 增加压缩开始、完成、失败和取消事件，前端改用后端真实 token 用量并显示独立“正在压缩上下文”状态。针对 DeepSeek-V4-Flash 的 1M 官方窗口，产品默认 1000000 token、预留 65536、80% 触发、45% 目标；测试使用 256 token 窗口、50% 触发、25% 目标。验证覆盖后端路由/重复压缩/状态共存/失败回退、前端 29 项相关单测、Vite 构建及 Chromium 生命周期冒烟。
- [x] 上下文压缩
  - 加载历史的时候上下文显示莫名其妙的到达100%?上下文长度是怎么计算的?有没有同步压缩机制?显示了100%之后为什么没有触发压缩机制还能对话?同步压缩有没有在前端显示为一个独立的仿工具条"正在压缩上下文"?

- [x] Agent的流式输出仍然有问题.
  - Agent经过较长的工具调用之后流式输出变得超大块并且停滞直到最后瞬间输出所有输出的bug.
  - 悬浮窗的流式输出几乎不存在的bug.
  - 悬浮窗在以上问题基础上甚至会卡死的bug.
  - 小窗的Agent没有跟Agent页面进行同步的bug.(小窗在一个对话后问了新问题,Agent页面仍然显示老对话)
  - 悬浮窗的组件没有和侧边栏模式Agent(尤其是顶栏)进行同步的问题.
- 工具注册表改为以 Agent 最终运行时注册表为全集，设置分组仅提供分类和开关状态；新增内置工具、MCP 工具或其他未分组工具自动进入“运行时工具”，不再因设置分组未同步而被隐藏。
- 新增只读“全局常量”子页面与 `/debug/global-constants` 接口；后端通过 dataclass 反射动态枚举 AgentConfig 全部配置组和字段，从配置类 docstring 提取介绍，并递归序列化当前值，后续新增配置字段无需维护前后端字段清单。
- Vite 统一登记 `/debug` 代理；新增后端动态覆盖测试、前端 API/组件/页面测试和 Chromium 界面冒烟测试。
- 全局常量页改为完整复用工具注册表的信息架构与组件规格：配置类作为可折叠分类、常量作为条目、右侧展示单条详情；两页统一读取 `registry-panel.css`，标题、搜索、双栏、列表、详情表格、代码块和响应式断点不再分别维护。
