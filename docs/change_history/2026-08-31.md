# 2026-08-31

## 编辑器小图标统一为 DSH + Morphicons

- 从本地 `deepseek-harness` 提取并内置 70 个 `ic_ds_*` SVG，不修改应用 Logo、无底图标或明暗标题图。
- 保留 `IcIcon` 语义接口，52 个严格匹配项使用 DSH，其余语义使用本地 Morphicons + Lucide 数据，完整覆盖原有 99 种小图标。
- 发送/停止、播放/暂停、显示/隐藏、复制/完成及展开/收起等关键状态切换改为 MorphIcon 变形；左侧边栏顶部保留原有明暗主题按钮视觉。
- PDF 工具栏与 Markdown DOM 增强图标并入统一入口，移除运行代码对旧 `ic` SVG 和 `lucide-vue-next` 的依赖。
- 验证：图标定向测试 5/5、MarkdownContent 回归测试 11/11、Vite 生产构建通过；桌面端与移动端实际页面冒烟无空图标或运行时异常。

- [x] 新加入更多的图标
  - 把图标全都换成DSH(D:\Projects\Python\deepseek-harness)的图标.本地下载.
  - 关键的状态切换的图标换成morphicon图标,本地下载.

## DSH 代码子 Agent、按需 Runtime 与只读 Web

- 新增 `provider=dsh` 的专用代码子 Agent路由，固定 Python SDK客户端、稳定 DSH Session、连续追问、冷恢复元数据、权限映射、Runtime容量与应用退出回收均接入现有 ChildAgentManager和正式子 Session。
- 新增 DSH Runtime受管资源服务与设置 API，支持 EXE内置固定 manifest与 ZIP、SHA-256、可选 Authenticode、真实解压进度、原子安装、修复、占用保护和卸载；约 66.0 MB的 Node 24生产闭包进入 MW主 EXE，首次使用时懒解压，安装后约 192.3 MB。
- 新增“存储管理—SDK 与运行组件”真实管理区块；Agent页仅对 DSH类型子 Agent打开同一 Runtime的 loopback Web，普通子 Agent继续使用原只读对话抽屉。
- 新增 Windows Job Object launcher、DSH `mw` profile、`session/open`/`session/flush`协议与 Web服务端只读栅栏；实际 Runtime冒烟已确认 Web写请求返回 `403 DSH_WEB_READ_ONLY`。
- 新增 DSH子会话数据库迁移、OpenAPI/数据库快照、后端服务测试、前端 API/组件测试和 DSH协议/连接测试。
- DSH Windows Runtime构建新增上游提交、Runtime版本和 Node主版本锁定校验，manifest记录源码仓库与完整提交；README增加锁定版本徽章，并明确 SDK仅由一键生产脚本构建、用户侧零构建环境及当前实测体积。
- 新增 `scripts/build_dsh_sdk.bat` 一键生产入口和 DEVELOPMENT使用说明；PyInstaller强制校验并内置固定 SDK制品，缺失、损坏或版本漂移时直接构建失败，运行时不再访问网络获取 DSH。
- README源码与运行时文件树同步展示已入库的 DSH SDK ZIP、manifest、上游版本锁、构建脚本、原生 launcher、Adapter/Runtime服务以及首次使用后的懒解压目录。
- README在 coding agent子智能体入口直接标注锁定的 DSH仓库提交和 MW Runtime版本；MW仓库新增可审核源码补丁，一键脚本改为自动克隆锁定提交、在临时目录应用补丁并构建，EXE门禁同时校验补丁哈希。
- 基础设置新增默认关闭、后端持久化的“启用 DSH”开关；开启后在应用界面启动完成时独立后台安装 Runtime，关闭时拒绝创建和继续 DSH子 Agent但保留已安装资源。
- 修复 Embedding与 ReRank后台线程并发首次导入 `sentence_transformers`触发 `_ModuleLock`死锁的问题：统一串行依赖导入，并确保任意导入异常都把模型状态从 loading收敛到 error。
- 存储统计、模型、编译器和 SDK管理读取改由 FastAPI线程池独立执行；前端各管理区拥有独立超时、错误与失败重试，一个慢磁盘扫描或请求取消不再连带其他区块。
- 主 Agent每轮运行时系统提示新增 native/dsh两类子 Agent目录、DSH用途、绝对 workspace_root要求及当前用户启用状态；simple、react、plan模式保持一致，关闭时明确回退 native。
- 精简主 Agent全量提示：移除静态与动态 DSH重复、重复图片/引用规则、跳号编号和空残句；静态段只保留输出、上下文获取、Task List与 Todo核心约束。
- 子 Agent提示集中为单一真实 `agent_type`目录：explore、dsh、coding；DSH启用时强制代码任务使用 dsh，未启用时才允许 coding后备，mode不再被描述成 Agent类型，category/provider从模型工具 schema隐藏。
- 子 Agent类型目录、DSH启用规则和coding回退规则全部迁入 `AgentConfig.PromptConfig`统一管理；运行节点只读取用户开关并调用配置格式化方法，不再在业务模块硬编码提示词正文。

## 灌库与图谱逐文件双队列

- 一键图谱抽取改为逐文件流水线：已灌库文件立即进入图谱队列，待灌库文件每完成一个便立刻追加一个图谱任务，不再等待整批灌库结束。
- 图谱抽取改为应用生命周期托管的动态队列；同一知识库串行写图，运行期间可并发追加请求，在途同一路径去重，应用退出时安全停止。
- 持久化灌库队列在重复全量提交时复用同一在途文件并追加新文件；前端允许多次提交并按本轮任务 ID 聚合入队、处理、出队进度。
- TopCommandBar 继续复用现有两条进度条，灌库和图谱总量均随队列增减实时重算；新增后端队列/REST与前端逐文件时序回归。
