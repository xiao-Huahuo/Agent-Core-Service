# 2026-08-20

- [x] 加速知识图谱抽取并适配并发进度：相邻短章节按最多 4 段、合计 12000 字符批量请求，小模型后台事实 worker 默认提升至 3；并发批次乱序完成时按已完成章节数单调更新文档和顶部总进度，保留逐章节实体、关系与证据归属。移除每章节 0.5 秒及每文档 1 秒固定等待，继续复用调度器的 429 重试、指数退避和并发上限。

- [x] 新增文件与图书馆隐私功能：以 SQLite `privacy_records` 表及 REST/gRPC 同步接口持久化隐私状态；文件树、资源管理器、图书馆补齐隐私列、按钮和“我的隐私”筛选，文件树与资源管理器右键菜单在“收藏”下方提供“隐私化”；普通视图和收藏视图默认排除隐私项，隐私清单加载失败时保持隐藏，隐私封面统一替换为占位；左侧新增仅含文件/图书馆的复用式隐私页，并补充 API、服务、组件与 Playwright 验证。

- [x] 修正文件树灌库/图谱右键动作：按持久化状态显示“重新灌库文件”与“重新抽取图谱”，两项相邻并移除“在图谱中显示”；图谱抽取复用已灌库结果，未灌库时等待单文件灌库成功后再抽取，显式重抽通过 `force` 绕过源哈希跳过。
- [x] 完善图谱抽取进度：轮询频率由 2 秒提升到 500ms，总进度纳入当前文档内部进度，逐文件展示 LLM 段落抽取、文档内去重、知识库去重和图谱写入阶段及阶段计数，顶栏同步显示文件与阶段进度。
- [x] 修复 Agent 流式 Markdown 在列表、表格和代码块输出时闪烁并延迟成段出现：移除每次增量后改写 Markdown DOM 的逐词动画与游标注入，保留 `marked` 和代码高亮对每批流式内容的即时渲染。

- 新增持久化单文件灌库任务：入库队列按文件显示管线阶段、百分比和页/行/工作表/幻灯片/图片/章节/切片/向量批次等真实工作单位；每个文件在独立进程中执行，可用“中止灌库”立即终止并清理该来源的部分索引，使其恢复未灌库状态。同步提供 REST、gRPC、前端轮询恢复及 Chromium 实际界面回归。
- 统一图书馆、组件库、密码库、智能表格、任务队列、用户反馈和设置页的普通输入框为灰色无细边框样式，并增加半透明聚焦 ring。
- 新增共享 `FormHeightTransition`，为图书馆状态表单、密码库、反馈、设置页和任务队列任务状态切换提供平滑高度适配；未改变业务/API 行为。
- 清理基础、外观、LLM、终端沙盒、记忆、密码与安全、存储管理及悬浮窗设置区的辅助文案与控件布局，复用资源管理器风格 toggle，并调整安全卡片、代码块和统计胶囊样式。
- 将设置页字体缩放调整为显示 100% 时沿用旧 120% 的实际字号，并将设置标签列统一扩展到约 8 个汉字宽度。
- 设置页 ring 卡片与存储统计胶囊增加统一左右内缩；终端允许程序目录改为带表头、分隔行和悬停态的密码库式表格。
- TopCommandBar 的搜索、浏览器、Git、Todo、图谱索引和灌库索引按钮统一为文件树顶部图标按钮样式；搜索移到主题切换左侧并隐藏 Agent 悬浮窗按钮。

- [x] 进一步统一化UI.
- [x] 外观微调:
  基础设置: 去掉所有的灰色小字;屏蔽区输入框改成代码块,高度扩大2倍; 屏蔽的文件类型胶囊改成图书馆胶囊样式;当前身份以及下面的文本去除掉.
  外观: 去掉所有的灰色小字;保存主题色按钮变为实心,重置主题色按钮的宽度要适应文字宽度;将图标栏/管理栏切换改成和文件资源管理器完全一致的toggle;
  LLM配置: 把小模型的灰色字移动到和小模型文本同一行的右边,不要单独占一行.
  终端沙盒: 去掉所有的灰色小字,将下方的cmd/ps/bash 改成和文件资源管理器完全一致的toggle,将其下面的所有内容都装进一个和任务队列一样的带有半透明边框的圆角卡片中.
  记忆与指令: 去掉所有的灰色小字. 
  密码与安全: 
    - 把所有的带有细线的边框的细线删掉并加上和那个一样的圆角和半透明边框; 将重置密码放到密码库卡片右下角,给重置密码的表单的输入框长度进行适配控制,样式完全向图书馆新建表单对齐,包括半透明边框;将获取密码和重设密码两个按钮放在密码库卡片的右上角.
    - 保存全部按钮放到安全审核词库文字那一行的右边;所有的添加精确匹配词输入框全都缩小2/3的宽度.
  存储管理: 左上角两个数字条改成圆角胶囊;
  悬浮窗: 去掉所有的灰色小字;指定模式三个切换改成资源管理器toggle样式和动效;启动小窗toggle右边的已关闭/已开启字样去除.
- [x] 将用户设置中的字体大小划分为UI字体大小和正文字体大小(编辑区显示的字体),分别调节.
- [x] 整治Agent侧边栏模式以及子组件样式,应该模仿移动端布局.
- [x] 新增模态: 视频,内置一个视频播放器.
  - 为Markdown预览提供<img>图片块和<iframe>视频块的预览.
- [x] 真正的反向链接: 
  - 给Markdown的右键菜单新增一个"显示反向链接"的选项,点击后编辑区底部冒出一个底栏显示链接当前文件的所有文件,并在每个文件下方标记其文本中链接是怎么写的,文件名右边标记其链接的是此文章还是文章的某个标题.
  - 用户设置里面也增加一个"显示反向链接"的勾选框,与右键菜单的选项效果一致.持久化.
  - 在readme里面补充反向链接的内容,修正原来描述不正确的地方.
- [x] 单文件的灌库进度条:和图谱抽取进度显示相似,在入库队列里面加一列灌库进度列进度条,根据不同文件类型显示不同的文件灌库进度.
  - [x] 提供一个"中止灌库"按钮,点击后立即终止此文件的灌库,仍然显示未灌库状态.
- [x] 库文件的隐私性: 新增隐私字段,类似于收藏字段. 标记为隐私的内容不显示封面.
    - 文件树点击顶部的显示...状态按钮之后,加上显示隐私化按钮的一列;在我的收藏按钮右边加上一个我的隐私,筛选隐私化文件.
    - 文件资源管理器列表模式加一列"隐私状态",顶栏"我的收藏"右边加一个"我的隐私",小中大图标的收藏按钮左边都要加上一个隐私化按钮.
    - 图书馆: 顶栏"我的收藏"右边加一个"我的隐私",卡片和条的右上角的收藏按钮左边都要加上一个隐私化按钮.
    - 左侧边栏新增页面"隐私",跟收藏页面的逻辑几乎完全一致,复用组件,只是锁死隐私状态而不是收藏状态.只显示文件和图书馆这两个子页面,不显示其他的.
    - 在readme里面相关处补充隐私功能.