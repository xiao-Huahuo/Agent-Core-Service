# 2026-08-18
- [x] 修复内置浏览器前进/后退按钮引用未登记图标键而渲染为空 SVG 的问题；浏览器原生 Chromium 主题同步到应用的暗色、亮色与跟随系统模式，使支持 `prefers-color-scheme` 的网页及 Chromium 原生界面随全局主题切换。补充组件与主题同步回归测试，并通过真实 Electron 图标及明暗媒体查询冒烟。
- [x] 为内置浏览器新增默认占据工作区右半屏的侧栏模式，并在应用顶栏增加小型浏览器开关；图书馆展示的 HTTP(S) URL 与 Agent 消息/工具输出中的 `[N]` 引用点击后均复用该侧栏和既有 Chromium Session 跳转，重复点击同一 URL 仍会重新导航，独立浏览器页面与侧栏模式互斥。验证：相关前端 22 项单元测试、ESLint、Vite 构建通过；真实 Electron 冒烟确认顶栏开关、左右 1:1 布局、图书馆 URL 与 Agent `[1]` 引用跳转均符合预期。
- [x] 新增工作区内置浏览器：左侧活动栏提供独立入口，现有大卡片内以 Vue 浏览器框承载隔离的 Electron `WebContentsView`，支持地址/搜索、前进后退、刷新停止、主页及系统浏览器打开；切离页面立即隐藏原生网页层。浏览器使用独立持久 Session、禁用 Node 与权限请求，仅允许 HTTP(S) 导航。设置页新增独立“浏览器”栏目，主页和代理持久化到用户配置，代理留空时继承联网搜索代理；REST、gRPC 与前端客户端字段保持一致。验证：Python 设置与 gRPC 13 项、前端 API/活动栏 11 项通过，ESLint 与 Vite 构建通过；真实 Electron 冒烟确认原生视图边界与卡片内容区一致、设置页可达且切页隐藏。
- [x] 为浏览器提供一个右侧边栏模式,默认占用半个页面.当点击图书馆的URL,或者在Agent页面中点击了[N]类型的引用时,则展开侧边栏并跳转到此链接. 为顶栏新加一个浏览器小按钮.
