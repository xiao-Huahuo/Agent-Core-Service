> 最高优先级规则：对用户提供的 TODO，必须**逐句**建立可验证的实现与验收对应关系,**必须**自己根据TODO反复逐句修改,**绝对禁止**自己随便实现一个长得像TODO但不符合用户的TODO的某一句的事情发生,每一句用户TODO都要在代码中有迹可循,每一句TODO都要遵守开发规范。禁止将任何一项降级为 MVP、替代实现或视觉近似；如无法复用用户指定组件，必须先说明阻塞并征得用户同意。完成前必须逐项进行实际界面冒烟验证，并报告未完成项。

### UI/UX

### TODOs
- [ ] 修DSH
### IDEAs
- [ ] 新增页面: 扫描器
  - 整体模仿MinerU的界面布局:
    - 左侧栏: 上方按钮:上传解析(打开系统文件),下面是一个一个"最近解析"块,块的外观和文件树的"最近浏览"块相似.标题下显示大小,左下角显示上传的时间,右下角不显示两状态,而是显示3个图标按钮:打开原文件所在位置,收藏,删除历史.点击则恢复此文件的解析结果页面.
    - 原页面:
      - 主区域: 一块巨大的文件上传区域,可拖拽,里面有一个无底图标png,下面2个按钮: 上传文件(打开系统文件,选择任何文件),网页链接(输入网页链接后使用爬虫爬下来,并作为markdown文档).区内右上角有个设置按钮,点击后下拉菜单包括: OCR(打开则启用ocr,默认开启).所有的下拉菜单应该复用文件树右键菜单的样式和动效.
      - 主区域下方: "示例",每一个示例块里面有一个图片,阴影悬浮,悬停时微旋转并可以超出示例块边界之外,上2/3是承载图片的区域,下1/3显示内容标题和tag.点击示例块可以直接开始解析此示例图片.
    - 解析过程中,主上传区的所有内容消失,中央显示一个像素loader,其下显示解析解析中,下面显示进度条,左侧栏显示此文件,其图标外要有一个转圈loader,标题下显示解析中.
    - 解析的本质是复用项目已有的"灌库"链,得到中间结果(markdown层),上传的文件应该放到.mw/scan里面.
    - 解析结果页:
      - 左侧显示上传的文件的编辑区(复用组件).
      - 右侧也是一个编辑区,但是显示此文件的解析结果markdown,且可编辑,
      - 顶栏显示一个回退按钮和文件标题,标题右边有一个复制(图标按钮),顶栏右侧的图标按钮依次是: 打开文件夹,收藏,保存(导出markdown到知识库内,文件名默认采用原文件名.md),导出(导出markdown到外部),复制(全文复制).
      - 解析时的ocr:
        - 若启用ocr,则右侧编辑区左上角显示一个toggle: ocr/no ocr,ocr的显示的markdown中全文皆文字,不显示图片.保存和导出时均为md文件;no ocr的则显示的是下面所说的,保存和导出也是下面所说的.不开启ocr则没这个toggle.toggle样式和动效复用文件资源管理器左上角的.
        - 若不启用ocr,则右侧编辑区的markdown的preview可显示图片.保存时则为md文件+文件同级目录的.assets/(默认)里面的图片(依照用户的基础设置里面的markdown图片目录).导出时则导出md+.assets/(默认)压缩为压缩包的zip.
  - 收藏页面新增扫描器子页: 同样的,只显示收藏的扫描器历史,尽量复用组件.
### BUGs
把扫描器的入口放在库agent按钮下面,并且应该换一个更合理的图标;
把左侧边栏顶部的按钮样式变为agent页面的历史侧边栏的新对话按钮样式和动效,并在其上方显示"扫描器"标题.
左侧边栏的外样式和抽动动效应该和agent页面历史侧边栏完全一致,圆角,无边框,位置都要一致.扫描器标题右边的折叠图标按钮也要一致.不过折叠的时候区别于agent页面在于展开侧边栏的入口应该是页面最左上角的一个展开按钮,样式和折叠按钮一致,但是图标是展开.
最近解析里面的块需要有出现动效,从上到下为逐个出现."还没有解析记录"应该放在侧边栏正中间.
来到解析区域,区域内左上角应该放一个文字"拖拽或上传",并且带有图标;点击解析区域任何地方都可以直接打开系统文件管理器,而不必等待拖拽进入.
填网页链接的表单的样式应该和图书馆新增表单完全一致,4px边框,圆角幅度,关闭按钮,输入框样式和动效,圆角按钮.
主上传区不应该上下滚动,要求的是响应式布局.
示例块中,悬停在块上的时候块也要发光,要有交互动效;每个图片的底部应该层次位于下半区域之下,无论常态还是悬停态,包括阴影也应该在其之下;压缩块的下半区域的高度,不要出现太多无文字空白区;去掉下半区域顶部边框;下半区域的tag应该和图书馆里面的tag样式一致.
示例块区域应该更活跃: 示例块区域应该禁止左右滚动,展示的块数目应该根据当前的页面宽度进行响应式自适应,并且像carousel一样轮播,每次轮播此宽度下的块,轮播要有左右移动动效.
示例块,最近解析历史块,和主上传区的圆角都应该和agent页面的历史侧边栏的圆角一致.
来到解析后的区域:
顶栏的高度应该和图书馆页面顶栏完全一致;
除了左右编辑区之间的边界,去掉所有的直线细边框.
对于左编辑区,顶部要显示编辑区组件的那些模式切换顶栏.复用组件,不要手写.
对于右编辑区,你似乎是重新手写了个新顶栏,这不是我们想要的.右侧编辑区也应该像左编辑区一样复用顶栏,我建议将ocr/no ocr的toggle弄到页面顶栏右侧.并且这个toggle的样式应该和项目内文件资源管理器页面左上角的toggle一致.
