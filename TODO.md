### TODOs
- [ ] 响应式布局,适配手机端.
### IDEAs
- [x] Markdown编辑区体验扩展:
  - [x] 提供在编辑区Edit模式直接插入图片的功能,即当剪切板为图片时,Ctrl+V或者右键粘贴按钮点击后自动注入一条`![]()`的图片链接,图片默认放在当前目录的`./assets/`目录下,并且用户可在设置中配置路径字符串.
  - [x] 对于Markdown表格:
    - 右键菜单中应该增加"插入行"和"插入列",二级菜单分别是"左侧插入|右侧插入"和"上方插入|下方插入".
    - 鼠标位于表格左边缘和上边缘时可按住一整行或一整列左右拖动;位于下边缘或右边缘时显示一个按钮(样式为一窄行/一窄列),点击后添加空行/空列
- [x] 智能表格扩展:
  - [x] 给文献上传表项右上角加一个下载原文件按钮和一个重新上传按钮,点击文献上传表项的效果改为跳转到编辑区打开原文件.
  - [x] 为文本和智能填充文本类的表项提供Markdown(包括Latex,代码块,表格等)的支持.(可行的方法是这些表项直接当做一个小编辑区Edit模式的阅读模式进行渲染)
  - [x] 表项交界可拖拽,以控制表格大小.
  - [x] 表项可以插入图片,图片自动上传到`forms/**/assets/`里面对应.(和在Markdown编辑区里面插入时一样的,都可以Ctrl+V直接插入图片,区别是表格的图片只能固定放在`assets/`里面而不可配置).图片同样具备右上角下载按钮.
  - [x] 智能表格也要有Markdown表格的边缘拖动和边缘插入的这些边缘按钮的逻辑.
- [ ] "库": 知识库可以有很多种表现形式,除了现有的文件资源形式,图书馆形式,还可以新的形式.
  - [ ] 密码库(模仿BitWarden):
    - 具有一个主密码,加密并绑定在用户id上.每次必须验证主密码才能进入,也就是说密码库需要开始具备真正的Auth逻辑(其他功能不受影响,仅密码库有密码).
    - 主密码的登录可以使用简单的JWT Token + HS256加密模式,Token存在前端,有效期30分钟,过期要求重新登陆,每个密码库接口都要具备JWT Token鉴权机制.用户可以主动"锁定",即去除Token并回到认证页面.
    - 密码分为几类: 登录,支付卡,身份,安全笔记. 没有写必填的就都是选填的.全都要配备增删改查.只能用户手动保存,不可自动保存.
      - 登录: 项目名称(必填),用户名,密码(必填),网站URI,图片,标签(可多个),备注,自定义字段. 
      - 支付卡:项目名称(必填),持卡人姓名,号码(必填),品牌(可选包括UnionPay银联,Visa,Mastercard,American Express,JCB,Discover,Diners Club,Maestro,RuPay,其他),安全码,图片,标签(可多个),备注,自定义字段.
      - 身份: 项目名称(必填),名字(必填),称呼(可选包括先生,夫人,女士,Mx,博士),用户名,公司,英文名,英文姓,电子邮箱,电话,国,省,市,区,地址(可以填3个),邮政编码,图片,标签(可多个),备注,自定义字段.
      - 安全笔记: 项目名称(必填),笔记内容(必填),图片,标签(可多个),备注,自定义字段.
    - 所有的密码全都存在数据库中,并绑定在用户和主密码上.图片则存储在runtime里面,不存储在知识库文件夹里.
    - 回收站功能: 和文件资源管理器的"最近删除"是一个东西,只不过全部在密码库鉴权之内.
    - 搜索功能: 可进行全字段联合匹配搜索,可进行已有的全标签筛选和密码类型筛选.
    - 导入导出: 可以将全密码库导出为JSON,也可以将部分多选的密码导出为JSON;导入JSON则将密码按密码类型分别补充进密码库. 如果遇到字段不匹配的情形,应该转化为安全笔记类型进行导入. 导入时要提醒用户成功导入了几个和有问题的有几个.
    - B端页面描述: 
      - 顶栏左边刻字"你的密码库",灰色字写"N个密码",右边有一个胶囊实心的"+ New",一个"密码库|回收站"toggle(样式完全模仿文件资源管理器左上角的toggle),一个导出图标按钮,一个导入图标按钮,一个圆形头像(随便从哪弄一个).
      - 有一个固定1/4宽度的左侧边栏(外观类似完整的悬浮表单,而不是简单的侧边栏),标题"筛选",从上到下是: 圆角搜索胶囊,无底按钮"+ New Password",一个横线隔开,最新的10个标签(用彩色胶囊,可展开),一个横线隔开,四个密码类型筛选按钮(登录,支付卡,身份,安全笔记),一个横线隔开,一个和看板左下角的饼图长得一模一样的展示各密码类型比例的饼图.
      - 主要区域是表格.对于列名,默认情况从左到右显示:图标(默认按照密码类型的图标,如果上传了图片则显示图片),项目名称,密码类型,创建时间,拥有者(显示user_id).
      - 密码的查看页面的话,按照各自的字段放几个长度合适的胶囊输入框或者圆角矩形输入区即可,图像上传区应该模仿图书馆页面里面新建文件表单的封面上传区块,这样就会更有结构感.
      - 表格右上角有一个多选按钮,点击后表格左边出现复选框.可以删除(移入回收站),可以导出为JSON.
      - 每一条密码都可以右键,右键菜单样式完全继承文件树的右键菜单(包括增加用户体验的三角区的逻辑),右键菜单有: 复制项目名称,复制密码(如果是登录或者支付卡),编辑,导出,删除.
      - 尽量复用项目其他地方的现有样式,防止用户发怒.
  - [ ] 组件库
  - [ ] 科研绘图脚本库
- [ ] 代码修改patch化,减少整文件覆写和误伤.在前端以代码形式工具修改md或者文本或者代码的时候要特别地用红蓝条标记显示出修改的局部代码.
### BUGs
