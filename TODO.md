# 最高优先级规则：对用户提供的 TODO，必须**逐句**建立可验证的实现与验收对应关系,**必须**自己根据TODO反复逐句修改,**绝对禁止**自己随便实现一个长得像TODO但不符合用户的TODO的某一句的事情发生,每一句用户TODO都要在代码中有迹可循,每一句TODO都要遵守开发规范。禁止将任何一项降级为 MVP、替代实现或视觉近似；如无法复用用户指定组件，必须先说明阻塞并征得用户同意。完成前必须逐项进行实际界面冒烟验证，并报告未完成项。

### TODOs
- [ ] 将用户设置中的字体大小划分为UI字体大小和正文字体大小(编辑区的显示),分别调节.
- [ ] 整治Agent侧边栏模式以及子组件样式,应该模仿移动端布局.
- [ ] 新增模态: 视频,内置一个视频播放器.
  - 为Markdown预览提供<img>图片块和<iframe>视频块的预览.
- [ ] 真正的反向链接: 
  - 给Markdown的右键菜单新增一个"显示反向链接"的选项,点击后编辑区底部冒出一个底栏显示链接当前文件的所有文件,并在每个文件下方标记其文本中链接是怎么写的,文件名右边标记其链接的是此文章还是文章的某个标题.
  - 用户设置里面也增加一个"显示反向链接"的勾选框,与右键菜单的选项效果一致.持久化.
  - 在readme里面补充反向链接的内容,修正原来描述不正确的地方.
### IDEAs
- [ ] claudecode子agent: 能不能把cc作为应用内的子agent进行操控调用,从而让agent获取代码能力?
- [ ] 单文件的灌库进度条:根据不同文件类型显示不同的文件灌库进度?
  - 提供一个"中止灌库"按钮,点击后立即终止此文件的灌库,仍然显示未灌库状态.
- [ ] 库文件的隐私性: 新增隐私字段,类似于收藏字段. 标记为隐私的内容不显示封面. 
- [ ] 上下文压缩
### BUGs