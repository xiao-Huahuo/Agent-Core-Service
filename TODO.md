> 最高优先级规则：对用户提供的 TODO，必须**逐句**建立可验证的实现与验收对应关系,**必须**自己根据TODO反复逐句修改,**绝对禁止**自己随便实现一个长得像TODO但不符合用户的TODO的某一句的事情发生,每一句用户TODO都要在代码中有迹可循,每一句TODO都要遵守开发规范。禁止将任何一项降级为 MVP、替代实现或视觉近似；如无法复用用户指定组件，必须先说明阻塞并征得用户同意。完成前必须逐项进行实际界面冒烟验证，并报告未完成项。

### UI/UX

### TODOs
- [ ] 给全库搜索加搜索框下的牌子
### IDEAs
- [ ] 调整agent对话区工具条的间距,不要显得那么宽.
- [x] DSH Adapter的实现
  - 文档位于`docs/ADAPTER_DESIGN.md`
  - 核心问题: 如何观测?
- [x] 图谱抽取逻辑优化:
  - 现状: 点击topcommandbar的一键抽取图谱之后,会将所有的可抽取文件先灌库,等全部灌库完成之后才开始抽取图谱.
  - 需求: 应该针对每个文件来操作,一个文件灌库完成就立刻开始抽取图谱,topcommandbar的两个进度条的进度应该实时计算,以任何一个文件的入队和出队为触发节点. 
  - 此外,还需要两个队列针对异步拥有良好的处理逻辑,应对多次点击抽取图谱和多次全量灌库拥有良好的并发追加.也要处理好顶栏进度条.
### BUGs
- [x] 上下文拼装过程和工具拼装过程有大量的硬长度截断,极大的阻碍了agent的思考,甚至让agent的思考过程转圈.
  - 1. 系统的找出所有的这些硬截断数字的根本来源.检查是否全部都登记在了全局Config里面,有没有漏的,有漏的就加进大部队.
  - 2. 把这些导致Agent上下文硬截断的上下文拼装和工具截断常数全都穷举出来.
- [x] 修提示词
  - 重写主 Agent 与检索上下文系统提示词，覆盖当前能力、执行边界、验证、记忆和引用准则。
  - 移除 `ModelConfig` 历史提示词副本，由 `PromptConfig` 作为唯一默认配置来源。
