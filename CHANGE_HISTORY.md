# Change History

## 2026-08-19

- 图书馆编辑表单新增“元信息/元文件”切换，支持文件、图片、网页 URL、纯文本和代码五种真实内容展示与对应访问行为。
- 纯文本和代码内容支持在元文件视图中编辑，并通过现有知识文件接口保存。
- 调整图书馆卡片/条形模式布局与编辑交互；统一封面、条形缩略图、下拉框和新增表单输入控件的圆角与边框表现。
- 浏览器右侧边栏增加抽拉过渡动效，并在侧栏顶部工具栏增加关闭按钮。
- 将浏览器侧栏改为纯位移动效，并将关闭按钮移入顶部活动标签块右侧。
- 将浏览器侧栏移到外层工作区网格，使主工作区大卡片像 Agent 侧栏一样被压缩，原生浏览器页面随独立栏位同步调整。
- 修正浏览器侧栏布局：改为外层独立网格列，避免原生页面停留在旧的右侧位置。
- 修正浏览器侧栏收起时的布局状态：保留侧栏列直到位移动画结束，避免工作区高度/布局提前坍缩。
- 将浏览器侧栏收起改为与 Agent 侧栏一致的单一外层网格同步过渡，移除额外位移与延迟卸载逻辑。
- 按 Agent 侧栏模式保留浏览器侧栏 DOM，通过网格列、透明度和横向位移同步折叠，避免动效消失及原生页面尺寸错乱。
- 将 Git 侧边栏顶部标题改为单行显示格式：`Git - 分支名`，保持已初始化分支与未初始化状态下显示策略不变。
- 文件树顶部按钮行将“更多工具”下拉按钮移到右侧位置，保持二级工具面板折叠/展开行为不变，减少页面高度突变。
- 优化文件树顶部“下拉”展开时的布局：将下拉触发按钮固定在第一排右侧，不参与第一排宽度重排，避免打开/收起时第一排位置抖动。
- 文件树默认恢复为两行布局：保留“最近浏览 / 收藏 / 列显示”第二行常驻显示并移除顶部下拉展开按钮。
- 文件树折叠面板展开动效改为更快的 `grid-template-rows` 过渡（130ms），去掉逐项入场延迟；添加树级横纵细线用于目录层级引导线条。
- 修复树节点“展开瞬间弹回”问题：`tree-children-wrapper` 的默认网格行基准改为 `1fr`，避免 JS 过渡结束后被默认样式回卷。
- 调整左侧“库”按钮 hover 命中区域：`knowledge-group` 的装饰性伪元素设为 `pointer-events: none`，避免悬停到文件树时误触发二级库菜单。
- 修复文件树层级细线不均：将层级细线从 `border` 改为固定 `1px` `background` 绘制（横向 `::before` 与纵向 `::after`），消除重叠处加粗感并统一粗细。
- 继续消除层级线并排变粗问题：移除 `.tree-children` 的 `border-left`，只保留 `tree-item` 的 `::before` 与 `::after` 细线作为层级连接线来源，避免文件节点出现双条并排细线。
- 按“菜单组件化”视觉样式补齐文件树行：减轻嵌套层级横向偏移（每层缩进收敛为 11px）并同步采用更轻量的字体/字号与图标样式（字体 14px、`font-ui`、16px 图标与统一 4px/8px 行内边距）以对齐目标体验。
- 按“替换组件”要求重写 `TreeNode.vue` 行级布局：从网格排布改为组件式行内 `flex` 结构（含 4px/8px 内边距、行高 28px、gaps 8px、字体与字号对齐参考示例），同时保持展开动画行为（含弹回修复）不变。
- 再次按你新贴组件“全量替换”`TreeNode.vue`：以纯粹 `.tree-label` / `.file-item` + checkbox 折叠结构替代旧节点壳，去掉状态列与其配套垃圾样式，保留行内编辑、拖拽、选择、右键事件链路，连线规则统一按 11px 偏移 + 1px 背景线。
- 2026-08-19：按用户提供的 checkbox 递归树组件整体替换编辑器文件树节点，统一文件夹开合动效、单一 1px 层级连接线、行布局与图标，并新增真实页面浏览器冒烟测试覆盖折叠、展开、层级线、选择和悬停。
- 2026-08-19：恢复文件树每个节点右侧的索引、图谱与收藏三状态及顶部开关联动；状态隐藏时直接移除对应布局列，使文件名完整占用释放空间，并补充浏览器宽度回归验证。
