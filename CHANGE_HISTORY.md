# Change History

## 2026-08-19

- 图书馆编辑表单新增“元信息/元文件”切换，支持文件、图片、网页 URL、纯文本和代码五种真实内容展示与对应访问行为。
- 纯文本和代码内容支持在元文件视图中编辑，并通过现有知识文件接口保存。
- 调整图书馆卡片/条形模式布局与编辑交互；统一封面、条形缩略图、下拉框和新增表单输入控件的圆角与边框表现。
- 浏览器右侧边栏增加抽拉过渡动效，并在侧栏顶部工具栏增加关闭按钮。
- 将浏览器侧栏改为纯位移动效，并将关闭按钮移入顶部活动标签块右侧。
- 将浏览器侧栏移到外层工作区网格，使主工作区大卡片像 Agent 侧栏一样被压缩，原生浏览器页面随独立栏位同步调整。
- 修正浏览器侧栏布局：改为外层独立网格列，避免原生页面停留在旧的右侧位置。
- 修正浏览器侧栏收起时的布局状态：保留侧栏列直到位移动画结束，避免工作区高度/布局提前坍缩。
- 将浏览器侧栏收起改为与 Agent 侧栏一致的单一外层网格同步过渡，移除额外位移与延迟卸载逻辑。
- 按 Agent 侧栏模式保留浏览器侧栏 DOM，通过网格列、透明度和横向位移同步折叠，避免动效消失及原生页面尺寸错乱。
