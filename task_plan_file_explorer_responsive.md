# Task Plan: 文件资源管理器三级响应式布局

## Goal
让文件资源管理器随联网搜索侧边栏拖动后的工作区大卡片实际宽度，在桌面、平板、移动三档下正确重排，并为每档提供实际截图。

## User Requirements and Acceptance Mapping
- [ ] 以“打开联网搜索侧边栏并拖动其左边界”作为主要测试路径，而不是只改变浏览器视口。
- [ ] 响应式判断跟随工作区大卡片的当前可用宽度。
- [ ] 文件资源管理器实现桌面、平板、移动三级布局。
- [ ] 三档均无横向溢出、截断、遮挡、重叠或异常空白，关键功能保持可用。
- [ ] 输出文件资源管理器三张实际截图，分别证明三个宽度。

## Phases
- [x] Phase 1: 定位页面、外层宽度变化链路和既有响应式模式
- [x] Phase 2: 先补充能暴露当前问题的自动化检查
- [x] Phase 3: 最小化实现容器级三级响应式布局
- [ ] Phase 4: 运行定向测试和回归检查
- [ ] Phase 5: 按用户拖拽路径完成三档界面冒烟与截图自证
- [ ] Phase 6: 审计差异、记录必要变更日志并交付

## Decisions Made
- 使用项目现有组件、主题变量和布局机制；不新增依赖。
- `@media` 不能单独证明侧边栏拖动后的容器响应，优先调查现有容器查询或宽度观测链路。
- 保留仓库现有未提交改动，不覆盖另一个任务的 `task_plan.md` 与 `notes.md`。
- 三级断点暂定为宽屏 `> 1040px`、平板 `641px - 1040px`、移动 `<= 640px`，最终以实际拖拽截图复核。
- 工具栏仅增加语义分组；列表与回收站单元增加语义类，数据和操作逻辑保持不变。

## Errors Encountered
- 非 PowerShell 的首次 Skill 读取命令引号解析失败；已改用可用的 UTF-8 读取方式完成读取。
- WindowsApps 中无扩展名的 `rg` 路径被解析为目录而拒绝访问；后续代码检索改用 `git grep`/`git ls-files`。
- 首次 CSS 原始文本测试经 Vite `?raw` 导入得到空字符串；改为与项目既有测试一致的 UTF-8 `readFileSync` 后，失败点准确指向缺失的容器查询。
- 首次 Playwright 冒烟停在全局加载态：通用 JSON 回退没有提供模型就绪状态；补充 `/health` 与 `/settings/models/status` 的真实启动契约后重试。
- 第二次 Playwright 已进入工作区，但 ActivityBar 的可访问名称是 `Files` 而非按钮提示文字“文件资源管理器”；按渲染快照改用真实可访问名称。
- 第三次确认 ActivityBar 的 `Files` 只切换文件树侧栏；改用主页“进入文件”入口打开文件资源管理器，保持真实用户导航。
- 主页“进入文件”在当前壳层状态下恢复了文件侧栏但未进入资源页；最终采用正式 ActivityBar 路径“库 → 文件资源管理器”。
- 知识库浮层带过渡动画，Playwright 等待按钮“稳定”期间浮层会重建；与仓库现有浮层测试一致，对已显示的菜单项使用强制点击。
- 无后端测试壳中资源页入口被现有侧栏状态逻辑接管；为避免修改无关导航，测试夹具仅直接设置 Pinia 主视图，侧栏打开与左边界拖动仍全部走真实 UI。
- Pinia 夹具首次读取过早，仅有 `settings` store；增加 `.workspace-page` 挂载完成等待后再选择资源视图。
- 控制台诊断显示通用 `{}` 回退破坏了 `/sessions` 与 `/git/status` 的数组契约；改为返回对应的空列表/完整空 Git 状态，并用合法 SSE 注释响应文件监听连接。
- 首次真正拖拽时主卡片被默认文件树和 Agent 两侧栏限制在约 488px；测试通过真实侧栏按钮先收起两者，以复现正式打开资源页的壳层状态。
- 拖动的单步 `pointermove` 与 `mouse.up` 落在同一帧，现有 `stopResize()` 会取消待执行的 rAF；改为多步拖动并在松手前等待一帧。

## Status
**Currently in Phase 4** - 运行定向测试并建立真实侧边栏拖拽截图用例。
