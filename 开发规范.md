# 开发规范

> **最高优先级规则：** 对用户提供的 TODO，必须**逐句**建立可验证的实现与验收对应关系,**必须**自己根据TODO反复逐句修改,**绝对禁止**自己随便实现一个长得像TODO但不符合用户的TODO的某一句的事情发生,每一句用户TODO都要在代码中有迹可循,每一句TODO都要遵守开发规范。禁止将任何一项降级为 MVP、替代实现或视觉近似；如无法复用用户指定组件，必须先说明阻塞并征得用户同意。除了小型UI修改,完成前必须逐项进行实际界面冒烟验证，并报告未完成项。写完代码后把自己产生的垃圾文档清理掉.
**特别针对Codex**:统一采用UTF-8编码,**禁止使用Powershell**,禁止将文件写中文乱码; 没必要总是在编写后重新编译,除非用户要求.
**内存安全红线**: 禁止直接运行全量测试、全量测试与构建并行或其他会一次性加载全部模型/前端 worker 的高内存验证，以免内存爆炸。测试必须按文件或模块拆分，使用单进程、单 worker、严格串行的低内存方式分批执行；每批结束并释放资源后才能开始下一批，最终根据全部分批结果汇总验收。
所有AI在执行了代码修改后都必须在`docs/change_history/YYYY-MM-DD.md`对应当天日期文件中书写代码修改日志；当天文件不存在时创建，并同步更新`docs/change_history/README.md`日期索引.(简介为主,简单的UI或者组件小改不用写)
所有AI都应该在任何可以写注释的地方写充分的注释,所有文件无论大小,都要在顶部书写文件功能与使用说明注释,所有的类和字段和函数都要充分的写注释来描述功能和使用方法.
所有的类和字段和函数都要充分的写注释来描述功能和使用方法.所有的组件都要写明其使用场景.
任何的代码修改都要遵循**测试寻bug**的原则,首先找到问题所在,并通过测试了解你找到的问题到底是不是问题,然后再修,最后自行测试看看改之后是否解决了问题,且没有制造其他的bug.
所有Agent都应该使用ponytail这个skill写代码,这个skill可以帮助减少大量的废代码,并极大地进行代码复用.
**特别注意**: !!!**任何AI,自己开的端口,必须自己关,每次最终回答之前必须关掉自己开的所有端口**!!!
### Vibe Coding 八荣八耻
以暗猜接口为耻，以认真查阅为荣。  
以模糊执行为耻，以寻求确认为荣。  
以盲想业务为耻，以人类确认为荣。  
以创造接口为耻，以复用现有为荣。  
以跳过验证为耻，以主动测试为荣。  
以破坏架构为耻，以遵循规范为荣。  
以假装理解为耻，以诚实无知为荣。  
以盲目修改为耻，以谨慎重构为荣。
### 前端
1. 所有组件均需划分为页面组件与子组件,分别放置在`src/views`与`src/components`目录下,页面组件需使用大驼峰命名法,子组件应当存储于相应的页面的蛇形命名法目录下,并使用大驼峰命名法命名组件文件,组件文件内的组件名称也应当与文件名保持一致,如:
```
src/
├── views/                  # 页面组件（路由直接指向的组件）
│   ├── UserProfile.vue     # 大驼峰命名
│   └── HomeView.vue
│
└── components/             # 子组件（被页面或其它组件引用的部分）
    ├── user_profile/       # 对应页面的蛇形命名文件夹
    │   ├── UserInfo.vue    # 大驼峰命名，与文件名一致
    │   └── SecuritySet.vue
    └── common/             # 全局通用组件
        └── CustomButton.vue
```
2. 所有API路由与请求拦截器均登记于`src/router/api_routes.js`文件中,页面路由和路由守卫和vue-router相关的逻辑均登记于`src/router/index.js`中,以便于统一管理和维护。禁止自己乱放路由.
3. 全局配色切换统一由`src/stores/settings.js`管理: 使用`theme_mode`控制明暗主题,通过给`document.documentElement`设置`data-theme`实现`light`/`dark`/跟随系统切换; 使用`color_scheme`控制全局配色,通过设置`data-color-scheme`在`src/assets/main.css`与`src/assets/ui-system.css`中消费对应主题变量.禁止绕开该机制自行编写非全局配色,一切的配色都要在`settings.js`中登记.
4. 全局字体切换统一由`src/stores/settings.ts`管理: 使用`ui_font_families`控制界面字体家族,使用`text_font_families`控制文字字体家族,通过设置`document.documentElement`上的字体CSS变量在`src/assets/main.css`与`src/assets/ui-system.css`中消费,非必要禁止绕开该机制在组件内硬编码全局字体,原默认字体栈必须保留为fallback.
5. 登录注册页面统一使用Home的登录注册组件,禁止自己编写其他的登录或者注册页面.
6. 所有的组件应保持高可复用性,组件/布局/样式,写页面和组件的时候应该首先调查前端其他页面和组件,尽量在样式上保持一致.能复用绝不重写,能抽象绝不重复,能组件化绝不冗余.禁止一次性写入超过1000行的同一个文件,如果遇到需要写入超过1000行的情形也应该拆成子组件.
7. 新增或修改前端调用的后端接口时,必须同步检查并更新`editor/vite.config.ts`中的Vite开发代理配置,确保新增接口前缀能从前端开发端口正确转发到后端;提交前必须通过前端开发端口对新增接口执行一次真实请求验证,禁止只测试后端端口而遗漏开发代理导致404.
8. 任何关联后端的功能业务修改,都要对前端部分进行完备的前端 API 客户端单元测试（HTTP 请求构造测试）,绝对禁止在**纯前端里面塞假数据糊弄用户**或者**放个按钮不接借口**这样的敷衍行为(甚至美其名曰MVP/第一版).
9. 去AI化,少用胶囊标签,禁止在大字旁边或者下面放灰色解释性小字,禁止使用一个带有边框或者阴影的圆形或圆角矩形来包裹一个图标按钮(小按钮尽量无边框无阴影),禁止用圆角矩形包裹一行文字和一个数字这种丑陋的数字陈列. 简约为主,实用为主,以提升用户体验为目标,以编写不必要的垃圾文本为耻. 避免一切的AI味组件.
10. 严格贯彻强响应式布局,遵守三级响应式布局(“桌面端 / 平板端 / 移动端”,以及超窄模式的一些基本使用考虑),按照交互逻辑重要性与UX进行组件选择性呈现,必要时可按照不同布局研发出不同的组件.涉及响应式布局的部分,必须截图,通过Agent自身的视觉观察通过(布局合理,交互合理,组件无截断或缺失,无莫名空缺)后才能验收.
注: 以上规则中,`js`可以视情况改为`ts`.

##### editor前端设计
  遵照`awesome-design-md`来设计,具体设计如下:
    统一字体设计: 界面字体system-ui,代码字体JetBrains Mono,
    主色调: 红蓝混色色调,蓝色作为主色调::蓝宝石#4224EB,红色作为点缀色点:三角色#EB2463. (注: 用户可调节)
    骨架: [VoltAgent](https://getdesign.md/voltagent/design-md),VoltAgent 只参考布局骨架、暗色层级和组件密度，不继承其绿色主色。
    编辑区: [Cursor](https://getdesign.md/cursor/design-md)
  支持全局明暗切换、字体切换、字体大小控制,明色系为主.
  除主色和点缀色外，只允许语义状态色用于索引、错误、成功、警告，不得作为装饰色大面积使用。
  避免霓虹赛博、营销大卡片、过度装饰和低密度首页化设计。
  需要适配响应式布局.

### 后端
0. 持久化红线: 任何用户级、业务级、跨设备或需要被后端/多端一致读取的数据, 必须通过正式后端模型、服务层、数据库表和迁移脚本持久化, 禁止用浏览器存储(localStorage/sessionStorage/IndexedDB)、前端内存状态、随手 JSON/YAML/文本文件、临时脚本注入等方式冒充业务持久化。只有在数据明确属于纯 UI 临时偏好、可丢弃缓存、调试探针, 或用户明确要求本地一次性保存时, 才允许使用浏览器存储或文件型临时存储; 使用前必须在实现说明中写明原因、生命周期和清理/迁移策略。遇到收藏、会话、用户设置、知识库元数据、权限、同步状态等业务数据时, 默认必须走后端 API + 数据库, 不得擅自降级为前端本地存储或 JSON 文件。
1. 配置采用"服务级默认 + 用户级覆盖"双层模型：`AgentConfig`（环境变量/.env/dataclass默认值）提供进程级保底值且启动后只读，`user_settings` 表提供运行时可变的用户级覆盖值；读取端（API）必须遵循"用户值 or 服务默认值"的降级合并策略，写入端仅持久化用户显式覆盖的字段，禁止将服务默认值写入用户表。
2. 模型层规范:分为`models`和`schemas`两个模块,前者存放XXXBase(基础模型)和XXX(数据库模型),后者存放三个DTO(如:XXXCreate(创建DTO,继承XXXBase),XXXUpdate(更新DTO,继承SQLModel),XXXOut(响应DTO,继承XXXBase)).必要时可以适当变通.
3. API规范:如果改了rest接口,应该同步修改grpc接口,反之亦然.要让外部主服务能用grpc端口复现出一个面板出来.
4. 禁止治标不治本的偷懒行为,比如遇到存储型问题用bash**注入而不改代码**,比如在**纯前端里面塞假数据糊弄用户**,比如**放个按钮不接借口**,比如直接在代码里面**硬编码**测试用例,或者找到问题**只汇报给用户但是不改**.这属于是绕过问题而不是解决问题,会导致把库删了仍然存不了,属于是**绝对禁止**的行为!!!!!
   **特别针对Codex**: 放弃那不必要的所谓MVP和第一版第二版理念,用户的需求就是天,当用户要求实现一整个功能的时候,应该反复迭代,反复全项测试,直到100%符合用户的需求! 
### 部署运维
没有其他事不要瞎几把乱动`Dockerfile`和`docker-compose.yml`,如有需要(如增加环境变量,改端口服务)才能编辑,并且需要在`docs/change_history/YYYY-MM-DD.md`对应当天日期文件中说明修改内容和原因,以便于团队成员了解和审核.
